# 지호 작업 계획 (v0.3 문서 반영)

> 06.md v0.3 · 07.md v0.2 · 08.md v0.3 기준
> 오늘: 2026-04-23 (목)
>
> **⚠️ 작업 대상: 주간 리포트 + 메인 대시보드만**
> 하루루 agent (v14 운영 중) · 기존 `/` HomeHero · `src/components/haruru/*` · 나경/슬아/정민/진희 팀원 영역은 **건드리지 않음**

---

## 0. 현재 상태 스냅샷

### ✅ 이미 완료 (v0.2 문서 기준으로 배포됨)

- 마이그A: `hotpack_llm_reports.kind` CHECK에 `weekly_brief` 추가
- 마이그B: RPC 3개 (`can_generate_weekly_brief(boolean)`, `get_current_report_template`, `upsert_weekly_brief_chunks`)
- 마이그C: `haruru-audio` 버킷 + RLS + cleanup cron
- Edge Function 3개 v1 배포: `generate-weekly-brief`, `generate-weekly-audio`, `transcribe-audio`

### 📂 현장 확정 자산

- **루트 `briefing.css`** (16KB) — 슬아 디자인 시안. 완전한 `.hb-*` 토큰·게이지·플로우·insight 3색·스파크라인·체크리스트·CTA 다 포함. **재작성 불필요, 이관만** 하면 됨. CTA만 주황 변경.

### ⚠️ v0.3과 불일치 → 재작업 필요

| 항목                                 | v0.2 현재                  | v0.3 문서                                    | 조치                 |
| ------------------------------------ | -------------------------- | -------------------------------------------- | -------------------- |
| `can_generate_weekly_brief` 시그니처 | `(p_force_monday boolean)` | **파라미터 없음, 월/금 가드**                | 함수 교체            |
| Reason 문구                          | "월요일만"                 | "월요일 또는 금요일"                         | 위와 함께 교체       |
| `next_available` 반환                | 없음                       | `this_monday/this_friday/next_monday`        | 위와 함께 교체       |
| `generate-weekly-brief` 로직         | RAG 검색 사용              | **RAG 검색 없음** — 최근 4주 요약만 컨텍스트 | 재배포               |
| 리포트 톤                            | 하루루 톤                  | **공식 사내 보고서 톤**                      | 시스템 프롬프트 교체 |
| § 2~7 쿼리                           | 자체 작성                  | **기존 뷰 재사용** (`v_orders_summary` 등)   | 쿼리 교체            |

### 🆕 v0.3 신규 요구사항

- `/dashboard` 경로 전체 신설 (기존 `/` HomeHero 건드리지 않음)
- 사이드바 `Sidebar.tsx` 수정: 최상단 "대시보드" 메뉴 + active 주황
- `src/components/dashboard/` 컨테이너 일체
- `src/components/briefing/` (슬아 디자인 재구현)
- `src/components/weekly-brief/` (14개 컴포넌트, 07 문서)
- `src/components/audio/`, `src/components/voice/` (Context 기반)
- `src/lib/demo/` (5개 프로파일 상수, peak만 채움)
- `src/contexts/AudioPlayerContext.tsx` (Zustand 대체)
- 주황 CTA `#F97316` 색상 토큰 일관성

---

## Step 1. 백엔드 v0.3 재정합 (30분)

### 1-1. `can_generate_weekly_brief` 재생성

- 기존 `(boolean)` drop → 새 `()` 생성
- 월요일(1) 또는 금요일(5)만 허용
- `next_available` 필드 추가

### 1-2. `generate-weekly-brief` v2 재배포

- `sqlSections.ts` 교체 — 기존 뷰 활용 (`v_orders_summary`, `v_hotpack_season_daily`, `v_current_stock`, `v_weather_hybrid`, `v_keyword_daily_with_ma`)
- `prompt.ts` 교체 — 공식 사내 보고서 톤 프롬프트
- `index.ts` 수정 — RAG 검색 제거, 대신 `hotpack_llm_reports`에서 최근 4주 weekly_brief 요약만 뽑기
- **Claude 호출 파라미터는 그대로 `claude-sonnet-4-6` 유지**

### 1-3. 검증 SQL

```sql
-- 오늘 목요일 → 월/금 아님 → allowed:false
select can_generate_weekly_brief();
```

---

## Step 2. 공용 인프라 · 디자인 토큰 (20분)

### 2-1. 파일 신설

```
src/
├── contexts/
│   └── AudioPlayerContext.tsx     🆕 Zustand 대체 Context + useReducer
├── lib/
│   ├── demo/
│   │   ├── types.ts                🆕
│   │   ├── seasonProfiles.ts       🆕 (peak 완전 / 나머지 4개 스켈레톤)
│   │   ├── demoContext.tsx         🆕 DemoDataProvider
│   │   └── index.ts                🆕
│   └── dashboard/weekly-brief/
│       ├── types.ts                🆕
│       ├── useWeeklyBriefGate.ts   🆕 TanStack Query
│       ├── useGenerateWeeklyBrief.ts 🆕
│       ├── useWeeklyBrief.ts       🆕
│       ├── useWeeklyBriefList.ts   🆕
│       ├── useVoiceInput.ts        🆕 STT 녹음
│       └── markdownRenderer.tsx    🆕 [ref:sql.row_N] 툴팁
```

### 2-2. 기존 파일 확인만

- `package.json`: Zustand 없음 확인 (grep)
- `@tanstack/react-query`·`react-markdown` 설치 여부 확인

---

## Step 3. 대시보드 쉘 (30분)

### 3-1. 라우트

```
src/app/dashboard/
├── layout.tsx                     🆕 DemoDataProvider + AudioPlayerProvider + Sidebar
├── page.tsx                       🆕 DashboardMain
└── dashboard.css                  🆕
```

### 3-2. 컨테이너

```
src/components/dashboard/
├── DashboardMain.tsx              🆕 Topbar → 브리핑 → 브릿지 → 주간리포트
├── DashboardTopbar.tsx            🆕
└── NarrativeBridge.tsx            🆕
```

### 3-3. 사이드바

- `src/components/layout/Sidebar.tsx` 수정
- NAV 배열 최상단에 "대시보드" 추가 (라우트 `/dashboard`)
- active = 주황 배경 `#FFF7ED` + 글자 `#C2410C`
- 모바일 <768 숨김 (Phase 2 햄버거)
- **주의**: 기존 `/` 랜딩 페이지 링크(`href="/"`로 가는 로고)는 그대로 유지

---

## Step 4. 브리핑 카드 (1시간, 단축)

### 4-1. CSS 이관

- 루트 `briefing.css` → `src/components/briefing/briefing.css`로 **복사**
- 파일 끝에 CTA 색상 override 블록만 추가:
  ```css
  /* v0.3: CTA 검정 → 주황 그라데이션 */
  .hb-cta-button {
    background: linear-gradient(135deg, #fb923c, #f97316);
    box-shadow: 0 4px 12px -2px rgba(249, 115, 22, 0.4);
  }
  .hb-cta-button:hover {
    background: linear-gradient(135deg, #f97316, #ea580c);
    box-shadow: 0 6px 16px -2px rgba(249, 115, 22, 0.5);
  }
  ```
- 반응형 미디어쿼리는 **추가** (briefing.css에 반응형 섹션 없음)

### 4-2. 컴포넌트 (React 마크업)

`briefing.css`의 클래스명을 그대로 소비. `hb-*` 클래스 구조를 기반으로 React JSX 작성.

```
src/components/briefing/
├── BriefingCard.tsx               🆕 .hb-card 쉘
├── BriefingHeader.tsx             🆕 .hb-card-header (badge + date + meta)
├── BriefingWeatherColumn.tsx      🆕 .hb-weather-* (기온·강수 6칸·트리거 3행·insight blue)
├── BriefingInventoryColumn.tsx    🆕 .hb-gauge-* Top3 + .hb-flow-* 스테퍼 + insight orange
├── BriefingActionColumn.tsx       🆕 .hb-task-* + .hb-search-* 스파크라인 + insight green
└── briefing.css                   🆕 (이관본 + CTA 오버라이드 + 반응형)
```

### 4-3. 데이터 주입

- 모두 `useDemoData().profile`에서 props drill
- DB 조회 없음 (Phase 1)
- peak 프로파일만 렌더

### 4-4. CTA 텍스트

- "상세 보기 및 발주 관리" + 화살표 SVG (기존 `.hb-cta-button > svg` 유지)

---

## Step 5. 주간 리포트 카드 (3시간)

### 5-1. 컴포넌트 (07 § 4)

```
src/components/weekly-brief/
├── WeeklyBriefCard.tsx            🆕 상태 A~E 머신
├── WeeklyBriefHeadline.tsx        🆕
├── WeeklyBriefAlerts.tsx          🆕
├── WeeklyBriefSectionChips.tsx    🆕 §1~§7 칩
├── WeeklyBriefFooter.tsx          🆕 gate 상태
├── WeeklyBriefGenerateCTA.tsx     🆕 하단 주황 큰 버튼
├── WeeklyBriefProgress.tsx        🆕 진행 프로그레스
├── WeeklyBriefHistory.tsx         🆕 최근 리포트 목록
├── WeeklyBriefModal.tsx           🆕 상세 모달 (?brief=<id>)
├── ReportSection.tsx              🆕 섹션 하나 (TTS 버튼 포함)
├── ReportSectionToc.tsx           🆕 사이드 목차
├── AskAboutReport.tsx             🆕 챗봇 열기 링크
└── weekly-brief.css               🆕 wr-* 토큰
```

### 5-2. 5개 상태 분기

- A: 금주 리포트 존재
- B: 리포트 없음 + 월/금 (주황 CTA 활성)
- C: 월/금 아님 (회색 비활성)
- D: 2/2 도달 (회색 비활성)
- E: 생성 중 (주황 프로그레스)

### 5-3. URL 동기화

- `?brief=<report_id>` → 모달 오픈
- `#section-<key>` → 스크롤

---

## Step 6. 오디오 · 음성 입력 (1.5시간)

### 6-1. 컴포넌트

```
src/components/audio/
└── AudioMiniPlayer.tsx            🆕 하단 sticky / 데스크톱 우하단 floating

src/components/voice/
└── VoiceInputButton.tsx           🆕 STT 녹음 토글
```

### 6-2. 핵심 원칙

- 전역 단일 오디오 인스턴스 (`AudioPlayerProvider` 대시보드 루트에 1회)
- 섹션 이동해도 재생 지속
- STT: 녹음 종료 후 `chunksRef.current = []` 즉시 비우기, 서버는 텍스트만 반환
- 자동재생 차단 (iOS) → 첫 재생은 반드시 사용자 클릭

---

## Step 7. 빌드 · 시연 검증 (30분)

### 7-1. 타입·린트

- `npx tsc --noEmit` 0 에러
- `pnpm lint` 0 에러

### 7-2. Dev 서버 확인

- `pnpm dev` → `/dashboard` 접속
- 브라우저에서 기능 확인:
  - peak 프로파일 렌더 (기온 -8℃, 군인 핫팩 180개 등)
  - 주황 CTA 클릭 → gate 반환 "금요일에 활성화됩니다"
  - 사이드바 active 주황 톤
  - 모바일 DevTools 375px → 브리핑 1열 + CTA 전폭

### 7-3. 기존 `/` 회귀 테스트

- HomeHero 그대로 동작
- 하루루 챗봇 정상 스트리밍

---

## Step 8. 커밋 단위 (5회)

| #   | 커밋 메시지                             | 포함 파일                         |
| --- | --------------------------------------- | --------------------------------- |
| 1   | `[PM] 주간리포트 백엔드 v0.3 정합`      | 마이그 + Edge Function 재배포     |
| 2   | `[PM] dashboard 라우트 + 사이드바 메뉴` | `/dashboard`, Sidebar.tsx         |
| 3   | `[PM] 브리핑 카드 + 5개 시즌 프로파일`  | briefing/_, lib/demo/_            |
| 4   | `[PM] 주간리포트 UI 14 컴포넌트`        | weekly-brief/_, audio/_, voice/\* |
| 5   | `[PM] AudioPlayerContext + 빌드 검증`   | contexts/\*, 빌드 통과 로그       |

---

## 9. 팀원 영역 경계

| 영역                                                 | 수정 여부         | 근거                                         |
| ---------------------------------------------------- | ----------------- | -------------------------------------------- |
| `src/components/orders/` (슬아)                      | **건드리지 않음** | 슬아 영역 · briefing.css는 루트에서 복사만   |
| `src/components/analytics/` (정민·나경)              | **건드리지 않음** | 팀원 영역                                    |
| `src/components/logistics/` (진희)                   | **건드리지 않음** | 팀원 영역                                    |
| `src/components/haruru/` (PM 기존)                   | **건드리지 않음** | 하루루 agent v14 운영 중, 06~08 작업 범위 밖 |
| `src/components/layout/Sidebar.tsx` (PM)             | 수정              | PM 영역 · "대시보드" 메뉴 추가               |
| `src/app/page.tsx` (PM)                              | **건드리지 않음** | 기존 HomeHero (하루루 랜딩) 그대로           |
| `src/app/(dashboard)/page.tsx` (PM)                  | **건드리지 않음** | 현재 HomeHero 렌더 중, 계속 유지             |
| `src/app/dashboard/` (PM 신설)                       | 전체 신설         | 신규 라우트 `/dashboard`                     |
| `supabase/functions/haruru-agent/`                   | **건드리지 않음** | v14 운영 중                                  |
| `supabase/functions/generate-weekly-brief/` (기배포) | 재배포            | v0.3 정합 (Step 1)                           |

---

## 10. 외부 의존성 확인 리스트

- [ ] `SUPERTONE_API_KEY` Supabase Secret 등록
  ```
  npx supabase secrets set SUPERTONE_API_KEY=696a3b14595e6ebea18ed929083dfa37 --project-ref sbyglmzogaiwbwfjhrmo
  ```
- [ ] Supertone voice_id 확정 (현재 `supertone:ko-neutral-formal` 플레이스홀더)
- [ ] `ANTHROPIC_API_KEY`·`OPENAI_API_KEY` Secret 확인 (이미 있음)
- [ ] `@tanstack/react-query` 설치 여부 확인

---

## 11. 예상 소요

| Step                                   | 시간         |
| -------------------------------------- | ------------ |
| 1. 백엔드 재정합                       | 30분         |
| 2. 공용 인프라                         | 20분         |
| 3. 대시보드 쉘                         | 30분         |
| 4. 브리핑 카드 (css 이관 → 1시간 단축) | 1시간        |
| 5. 주간리포트 UI                       | 3시간        |
| 6. 오디오·STT                          | 1.5시간      |
| 7. 빌드 검증                           | 30분         |
| **합계**                               | **약 7시간** |

---

## 12. 결정 대기

1. **Step 1부터 바로 진행할까?** (Y/N)
2. **5개 프로파일 중 peak만 완전, 나머지 4개는 타입 스켈레톤**으로 진행? (08 § 5-2 Phase 1 방침)
3. 커밋은 **5개 단위 순차** vs **한 번에 번들** 중?
