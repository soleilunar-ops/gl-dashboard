# 진희 작업 로그

> AI와 함께 작업한 내용이 자동으로 기록됩니다.
> 작업 완료 후 AI에게 "로그 남겨줘"라고 말해주세요.
>
> **누적 규칙 (2026-04-18):** 채팅에서 논의·결정·구현한 내용은 **작업 단위로** 아래와 동일한 형식(`### [날짜] [태그]` / 요청·변경 파일·변경 내용·주의사항)으로 **본 문서 상단(최신이 위)** 에 계속 추가합니다. 다른 채팅 세션에서도 동일하게 적용합니다.

---

<<<<<<< HEAD

### [2026-04-19] [ERP크롤·종료일수동조절제거]

**요청:** ERP 기간 설정에서 이전에 추가했던 날짜 수동 조절 기능 제거
**변경 파일:** `src/components/logistics/ErpCrawlPanel.tsx`, `docs/logs/진희.md`
**변경 내용:** 조회 종료일(`dateTo`) 입력 필드의 `onChange`/`setDateTo`를 제거하고, 시작일과 동일하게 `disabled`/`readOnly`/`opacity-60`으로 비활성화해 오늘 날짜로 고정 표시되게 함.
**주의사항:** 종료일은 컴포넌트 마운트 시점의 `getTodayDate()` 값으로 고정되므로, 자정을 넘겨 화면을 계속 띄워두면 이전 날짜로 남아있을 수 있음.
=======

### [2026-04-19] [밀크런·Prisma제거·allocations API·문서]

**요청:** Prisma 제거 및 `/api/allocations` 정리(서버용 Supabase로 대체); `docs/logistics-menu-guide-and-pm-brief.md`·`docs/logs/진희.md`에 변경 반영
**변경 파일:** 삭제 `src/lib/db.ts`, `prisma/` 전체; `package.json`·`package-lock.json`(`@prisma/client`·`prisma`·`db:generate`/`db:migrate` 제거); `src/app/api/allocations/route.ts`, `src/app/api/allocations/[id]/route.ts`(Supabase `createClient` + `getUser` 401, `SupabaseClient` 캐스트로 `allocations`·`allocation_items` CRUD); `src/components/logistics/_hooks/useMilkrunAllocations.ts`(상단 주석); `docs/coupang-milkrun-features.md`, `docs/logistics-menu-guide-and-pm-brief.md`, `.env.example`(밀크런은 `NEXT_PUBLIC_SUPABASE_*` 중심 안내); `docs/logs/진희.md`(본 항목)
**변경 내용:** 밀크런 저장·기간별 조회의 단일 진실은 계속 **`public.allocations` / `allocation_items` + 브라우저 훅**이며, REST API는 **Prisma·`DATABASE_URL` 없이** 동일 테이블에 세션 기반으로 붙도록 재구현. 레포 **Husky pre-commit의 prisma 금지 규칙**과 충돌하지 않게 의존성 제거. PM 문서에는 DDL 출처가 **`supabase/migrations`만**임을 명시하고, 과거 “Prisma + DATABASE_URL” 안내는 구버전으로 표기.
**주의사항:** `supabase/types.ts`에 테이블 미반영 시 API 파일의 `as unknown as SupabaseClient` 캐스트 유지; 나중에 `gen types`로 정리 가능. 과거 본 문서의 Prisma·`/api/allocations` 미사용 주의(2026-04-18 항목)는 **이번 변경으로 API는 다시 유지·Supabase화**됨 — 구 로그와 병행 시 날짜로 구분.

### [2026-04-19] [물류·쿠팡SKU분석·통합탭·GL탭·hydration]

**요청:** AI 보강 문구에서 Markdown·JSON 필드명·`쿠팑` 오타 제거 및 가독성; 분석 다이얼로그에서 규칙 인사이트 제거·차트 아래 재고 현황 분석·자동 생성·입고 막대·라벨 정리; 물류 페이지 GL 탭명·부제·GL 탭 차트 제거·필터 placeholder; 통합 탭 GL 표기·재고금액 카드→오늘 입출고·쿠팡 카드 `N SKU · M 센터`·쿠팡 발주 상태 카드·차트 제목; 쿠팡 목록 검색·3개월 업로드 안내·발주상태 열; `gl_warehouse_daily_series` 안내·API 친화 오류; hydration(`<p>` 안 Skeleton) 수정; 작업 로그
**변경 파일:** `src/lib/logistics/stripAiMarkdownNoise.ts`, `src/app/api/logistics/coupang-sku-narrative/route.ts`, `src/app/api/logistics/gl-daily-series/route.ts`, `src/components/logistics/CoupangSkuAnalysisDialog.tsx`, `src/lib/logistics/coupangSkuInsightRules.ts`, `src/components/logistics/CoupangFcInventoryTab.tsx`, `src/components/logistics/LogisticsPage.tsx`, `src/components/logistics/LogisticsGlTab.tsx`, `src/components/logistics/LogisticsUnifiedTab.tsx`, `src/components/logistics/FilterBar.tsx`, `src/components/logistics/GlWarehouseTrendChart.tsx`, `docs/logs/진희.md`(본 항목)
**변경 내용:** 쿠팡 SKU 분석은 `inventory_operation` 입·출고·팩트 JSON으로 차트·KPI·LLM 서술 정리(키 에코 후처리·프롬프트). 통합 탭은 최신일 행으로 박스 합·SKU·센터 수·발주 분류(일시중단/발주불가·그 외 발주상태) 집계; 쿠팡 탭은 검색·CSV 3개월 권장·테이블 열 `발주상태`. GL 일별 차트는 통합에만 두고 GL 탭에서는 제거. `<p>`+Skeleton hydration은 `div`로 교체.
**주의사항:** 발주 분류는 `order_status`/`detail` 한글 부분 일치 휴리스틱. RPC 미적용 시 GL 차트는 친화 메시지만 표시.

### [2026-04-19] [물류·3탭·쿠팡CSV·GL차트·입출고다이얼로그·GL품목코드]

**요청:** 창고 재고 UI 정리(엑셀 병합·설정 탭 제거, 7일 입출고 예정열 제거, 제조년도 표시); 쿠팡 일별 재고 CSV 업로드·센터별 목록·FC 명칭 정리; 물류 3탭(통합/지엘/쿠팡) IA·입출고 탭 제거하되 행 클릭으로 수불 유지; GL 일별 입출고+재고 추이 차트(7일·30일·기간설정); 쿠팡 센터 표에 GL 품목코드; 작업 로그 기록
**변경 파일:** `src/components/logistics/InventoryTable.tsx`(제조년도 열·7일 예정열 정리), `src/components/logistics/InventoryDashboard.tsx`(dailyInventoryBase 병합·안내 문구 제거), `src/components/logistics/_hooks/useInventory.ts`(7일 예정 필드 제거), `src/components/logistics/LogisticsPage.tsx`(3탭), `src/components/logistics/LogisticsGlTab.tsx`·`LogisticsUnifiedTab.tsx`·`GlWarehouseTrendChart.tsx`, `src/components/logistics/CoupangFcInventoryTab.tsx`, `src/components/logistics/_hooks/useCoupangInventoryByCenter.ts`(센터 필터·GL품목코드), `src/components/logistics/StockMovementLedgerPanel.tsx`(신규), `src/components/logistics/StockMovementsTab.tsx`(레거시 검색+패널만), `src/lib/logistics/parseCoupangRocketInventoryCsv.ts`, `src/app/api/logistics/coupang-inventory-upload/route.ts`, `src/app/api/logistics/gl-daily-series/route.ts`, `supabase/migrations/20260419180000_gl_warehouse_daily_series_rpc.sql`, `supabase/types.ts`(RPC `gl_warehouse_daily_series`), 삭제 `src/components/logistics/LogisticsSettings.tsx`, `docs/logs/진희.md`(본 항목)
**변경 내용:** (1) **총재고** — Supabase `v_current_stock`만 사용, 엑셀 스냅샷 병합 제거. (2) **설정 탭** — 미구현 API만 있던 `LogisticsSettings` 및 탭 제거. (3) **쿠팡** — 판매자센터 CSV 파싱 → `sku_master`·`inventory_operation` upsert API; 탭에서 센터 Select·행 목록; `item_erp_mapping`(gl 우선)으로 **GL 품목코드** 열 표시. (4) **물류 3탭** — 통합 요약카드, 지엘 탭에 Recharts 막대+우축 재고선·`InventoryDashboard`, 행 클릭 시 `Dialog`+`StockMovementLedgerPanel`로 입출고. (5) **차트 API** — `gl_warehouse_daily_series` RPC(orders 일별 입·출고) + `v_current_stock` 합으로 일말 재고 역산; `GET /api/logistics/gl-daily-series`.
**주의사항:** RPC 마이그레이션·`SUPABASE_SERVICE_ROLE_KEY` 미적용 시 차트·CSV 업로드 API 오류 가능. 재고 추이 선은 orders 역산 **참고치**. `StockMovementsTab`은 다른 경로용 레거시로 유지.

### [2026-04-18] [중국연휴·수동UI제거·내장만]

**요청:** 노란 표시 영역(수동 연휴 편집 문구·Supabase 테이블 없음 오류) 제거
**변경 파일:** `milkrun/PajuWeatherDashboard.tsx`, `milkrun/CnHolidayPeriodSettings.tsx`·`_hooks/useCnHolidayManual.ts`·`lib/cn-holiday-display-merge.ts`(삭제), `docs/logs/진희.md`(본 항목)
**변경 내용:** 중국 연휴 카드에서 수동 편집 UI·`cn_holiday_period_manual` 조회를 제거하고 `cn-official-holidays-builtin`만 표시. 스키마 오류가 나지 않음.
**주의사항:** 일정 수정은 `cn-official-holidays-builtin.ts` 코드 편집. DB 마이그레이션 파일은 저장소에 남아 있으나 현재 UI는 미사용.

### [2026-04-18] [중국연휴·Nager제거·국무원2026내장]

**요청:** 중국 공휴일 Nager API 제거, 수동 기입만 + 2026 국무원 발표 일정을 화면 기본값으로 표시
**변경 파일:** `lib/cn-holiday-period.ts`(신규), `lib/cn-official-holidays-builtin.ts`(신규), `lib/cn-holiday-display-merge.ts`(병합 단순화), `milkrun/PajuWeatherDashboard.tsx`, `milkrun/CnHolidayPeriodSettings.tsx`, `logistics/milkrun-weather/page.tsx`, `lib/nager-cn-long-weekends.ts`·`lib/nager-cn-holiday-ko.ts`·`api/holidays/cn/**`(삭제), `docs/logs/진희.md`(본 항목)
**변경 내용:** 2026 법정공휴일 연휴를 코드 내장(신정·춘절·청명·노동·단오·중추·국경 + 대체근무일 bridge); Supabase `cn_holiday_period_manual`은 동일 (연도·이름)이면 목록에서 덮어씀. Nager 연동 API 라우트 및 한글 매핑 유틸 삭제.
**주의사항:** 2027 이후는 내장 표가 없으면 수동 행만 표시. 다음 해 발표 시 `cn-official-holidays-builtin.ts`에 연도 추가.

### [2026-04-18] [중국연휴·수동설정·Supabase]

**요청:** Nager Long Weekend는 국무원 최종 공지와 다를 수 있으니, 설정으로 연휴 구간을 직접 조정·저장할 수 있게 함
**변경 파일:** `supabase/migrations/20260418160000_cn_holiday_period_manual.sql`(신규), `lib/cn-holiday-display-merge.ts`(신규), `lib/nager-cn-long-weekends.ts`(`source` 필드), `_hooks/useCnHolidayManual.ts`(신규), `milkrun/CnHolidayPeriodSettings.tsx`(신규), `milkrun/PajuWeatherDashboard.tsx`, `docs/logs/진희.md`(본 항목)
**변경 내용:** `cn_holiday_period_manual` 테이블(RLS·연도+이름 유일)에 수동 구간 저장; 표시 모드 `localStorage` — Nager만 / 병합(같은 한글 이름이면 수동 덮어쓰기) / 수동만; 파주 대시보드 카드에 설정·CRUD 다이얼로그 추가, 병합 시 수동 행에 `수동` 배지.
**주의사항:** Supabase에 마이그레이션 적용 전에는 테이블 없음 오류가 날 수 있음. Nager와 매칭하려면 수동 행의 이름을 API 카드에 나온 한글과 동일하게 맞출 것.

### [2026-04-18] [물류·파주대시보드·밀크런후속·진희md]

**요청:** 밀크런 기간별 시연 더미 제거·CSV를 DB 센터·메모 반영; 재작업일 날씨를 별도 메뉴로 분리; Nager.Date + 기상청 단기·중기로 파주 예보·중국 연휴 표시; 공휴일 한국어 및 며칠짜리 연휴 기간 표시; 작업 로그 기록
**변경 파일:** `milkrun/MilkrunHistoryTab.tsx`, `milkrun/milkrunHistoryDemo.ts`(삭제), `_hooks/useMilkrunAllocations.ts`(CSV export), `.env.example`(MILKRUN_MOCK 제거), `nav-logistics.ts`, `Sidebar.tsx`, `logistics/milkrun-weather/page.tsx`, `milkrun/PajuWeatherDashboard.tsx`(신규), `milkrun/MilkrunWeatherTab.tsx`, `api/weather/forecast/route.ts`(신규), `api/holidays/cn/route.ts`, `api/holidays/cn/long-weekends/route.ts`(신규), `lib/kma-short.ts`, `lib/kma-mid.ts`, `lib/kma-rework-fetch.ts`, `lib/kma-daily-weather.ts`, `lib/nager-cn-holiday-ko.ts`(신규), `lib/nager-cn-long-weekends.ts`(신규), `docs/logs/진희.md`(본 항목)
**변경 내용:** (1) **밀크런** — 시연 더미·`NEXT_PUBLIC_MILKRUN_HISTORY_MOCK` 제거; `listLinesForCsvExport`로 센터별·메모 포함 CSV. (2) **날씨 라우트** — `/logistics/milkrun-weather` + 사이드바 `재작업일 날씨`. (3) **파주 대시보드** — `buildPajuForecastRange`: 단기 격자 1회+중기 육상·기온 1회로 오늘~D+10(11일); `GET /api/weather/forecast`, `GET /api/holidays/cn`, `GET /api/holidays/cn/long-weekends`. (4) **중국 연휴** — `formatCnHolidayKo` 영·중 매핑; Long Weekend 구간과 PublicHoliday 대표일 매칭해 기간·`연휴 중`/D-day·연결일(bridge) 표시. (5) **기타** — `DailyWeather.label`을 `string`으로 확장; `MilkrunWeatherTab` 로딩 분기 타입 수정.
**주의사항:** 기상 예보는 기상청 API 한계로 최대 약 11일; 중국 연휴 구간은 Nager Long Weekend 모델이라 국무원 최종 공지와 다를 수 있음. Sidebar 아이콘 `Ship`·`Truck` 매핑 보강.

### [2026-04-18] [쿠팡밀크런·날씨탭제거·Supabase저장]

**요청:** `docs/coupang-milkrun-features.md` 기준으로 재작업일 날씨는 화면에서 빼고 비용 계산기·기간별 조회만 표시; 비용 계산기 저장은 수입 리드타임처럼 Supabase 클라이언트로 DB에 저장
**변경 파일:** `src/components/logistics/CoupangMilkrunPage.tsx`, `milkrun/MilkrunCalculatorTab.tsx`, `milkrun/MilkrunHistoryTab.tsx`, `_hooks/useMilkrunAllocations.ts`(신규), `supabase/migrations/20260418150000_create_milkrun_allocations.sql`(신규), `docs/logs/진희.md`(본 항목)
**변경 내용:** (1) **페이지** — 날씨 탭·`MilkrunWeatherTab` import 제거, 설명 문구 정리. (2) **저장/조회** — `POST/GET/DELETE /api/allocations`(Prisma·`DATABASE_URL`) 대신 `useMilkrunAllocations`에서 `allocations`·`allocation_items`에 insert/select/delete(인증 사용자 RLS). (3) **마이그레이션** — `public.allocations`·`allocation_items` + RLS 정책(`authenticated`). (4) **기간별 탭** — 테이블 없음 오류 시 안내 문구를 Supabase 마이그레이션 기준으로 수정.
**주의사항:** 기존 `/api/allocations` 라우트는 남아 있으나 UI는 사용하지 않음. `MilkrunWeatherTab.tsx`·`/api/weather` 등 날씨 코드는 파일만 남음(별도 페이지 연결 없음). 배포 시 `supabase db push` 또는 SQL Editor로 마이그레이션 적용 필요.

### [2026-04-18] [수입리드타임·MOCK제거·엑셀·진희md]

**요청:** 수입 리드타임에서 MOCK 모드·`NEXT_PUBLIC_LEADTIME_MOCK`·노란 안내 배너 제거 후 Supabase만 사용; Wings API 없이 하단 리스트를 엑셀로 추출하고 엑셀 버튼을 리스트 위에 배치; `import_leadtime` 미생성 시 안내·SQL Editor 적용 맥락 정리
**변경 파일:** `src/components/logistics/_hooks/useLeadTime.ts`, `src/components/logistics/LeadTimeTracker.tsx`, `.env.example`(MOCK 관련 줄 삭제), `docs/logs/진희.md`(본 항목)
**변경 내용:** (1) **`useLeadTime`** — `LEADTIME_MOCK`·시드 행·메모리 폴백·`isMockMode` 제거, 조회/추가/수정/BL/승인/삭제를 항상 `import_leadtime` 테이블 기준으로만 동작. (2) **`LeadTimeTracker`** — MOCK 배너 제거; 상단 우측의 “엑셀 추출”(Wings 알림) 제거 후 **`xlsx`로 현재 목록(`data`)을 시트 `수입리드타임`으로 저장**하는 버튼을 **테이블 바로 위**에 배치, 행 0건 시 비활성화, 파일명 `수입리드타임_YYYYMMDD_HHmm.xlsx`. (3) **DB** — 테이블 없을 때 PostgREST 오류(`Could not find the table 'public.import_leadtime'…`)는 정상이며, `supabase/migrations/20260418123000_create_import_leadtime_table.sql` 등을 Supabase SQL Editor에서 실행하거나 `supabase db push`로 pending 마이그레이션만 적용하면 됨(`DROP POLICY` 포함 시 에디터가 “destructive” 경고를 띄울 수 있으나 해당 마이그레이션 의도된 동작).
**주의사항:** `.env.local`에 남아 있는 `NEXT_PUBLIC_LEADTIME_MOCK`는 코드에서 더 이상 읽지 않음. `docs/logs/pm/import_leadtime.md` 등 구 문서에 MOCK 설명이 남아 있을 수 있어 필요 시 문서만 정리.

### [2026-04-18] [쿠팡밀크런·진희md·채팅누적]

**요청:** 이전 채팅에서 정리한 밀크런·Supabase·Prisma·PM 전달 문구 등을 `docs/logs/진희.md`에 기입하고, 앞으로 모든 채팅 내용도 로그에 남기도록 규칙 반영
**변경 파일:** `docs/logs/진희.md`
**변경 내용:** 상단에 “채팅·결정 사항을 상단에 동일 형식으로 누적” 규칙을 명시하고, 2026-04-18자 밀크런 관련 항목(재구성·삭제·DB연결·PM용 설명)을 본 문서에 한꺼번에 기록함.
**주의사항:** 자동이 아니라 요청 시·작업 종료 시 수동 누적이므로, 세션 마무리 때 “로그에 남겨줘”를 병행하면 누락이 줄어듦.

### [2026-04-18] [쿠팡밀크런·PM전달·Prisma요약]

**요청:** PM에게 DB 연결·마이그레이션을 어떻게 말할지, Prisma가 무엇인지 쉽게 설명해 달라는 요청을 문서에 남김
**변경 파일:** (본 로그 항목만)
**변경 내용 (PM 전달 예시):** “밀크런 배정 저장·기간별 조회는 Supabase Postgres에 테이블 2개(`allocations`, `allocation_items`)가 있어야 하고, `.env.local`/배포 환경에 **`DATABASE_URL`** 설정 후 `npx prisma migrate deploy`로 적용. API는 `/api/allocations`. 팀이 Supabase SQL만 쓰면 동일 DDL을 `supabase/migrations`에 올려 기존 파이프라인으로 배포 가능.”
**변경 내용 (Prisma 쉬운 설명):** “DB는 창고(데이터), Prisma는 창고 직원용 매뉴얼+도구(테이블 구조를 코드로 정리하고 TypeScript로 안전하게 넣고 빼게 해 주며, 테이블 생성/변경 SQL 묶음인 마이그레이션도 관리). 밀크런 저장은 브라우저용 Supabase JS 키가 아니라 **서버에서 Prisma로 Postgres에 직접** 붙는 방식.”
**주의사항:** `NEXT_PUBLIC_SUPABASE_*` 는 다른 기능용일 수 있어, 밀크런 저장만 보면 **`DATABASE_URL` + migrate** 가 체크리스트 핵심.

### [2026-04-18] [쿠팡밀크런·Supabase연결안내]

**요청:** 밀크런 비용 데이터를 Supabase에 저장하려면 연결을 어떻게 하면 되는지, 폴더 구조 기준으로 설명해 달라는 내용을 문서화
**변경 파일:** (본 로그 항목만)
**변경 내용:** 저장 경로는 **`DATABASE_URL` → Prisma → Postgres**. 폴더는 `prisma/schema.prisma`·`prisma/migrations/`(밀크런 테이블 DDL), `supabase/migrations/`(팀 공용 Supabase CLI 마이그레이션·현재 밀크런 DDL은 Prisma 쪽에만 있을 수 있음), `src/lib/db.ts`, `src/app/api/allocations/` 참고. Supabase 대시보드 **Database → Connection string(URI)** 를 `.env.local`의 `DATABASE_URL`에 넣고 `npx prisma migrate deploy` 로 테이블 생성. 비어 있으면 API 503 안내.
**주의사항:** Prisma migrate와 Supabase CLI 마이그레이션을 **둘 다** 쓰는 팀이면 중복 적용 방지를 PM과 맞출 것.

### [2026-04-18] [쿠팡밀크런·미사용코드삭제]

**요청:** 이전 발주·CSV·`MilkrunCenterCalculator` 등 더 이상 `CoupangMilkrunPage`에서 쓰이지 않는 코드 삭제
**변경 파일:** 삭제 `src/components/logistics/CoupangMilkrunDialogs.tsx`, `MilkrunCenterCalculator.tsx`, `types/milkrun.ts`, `utils/milkrunCalc.ts`, `constants/coupangCenters.ts`, `constants/coupangProducts.ts`, `_hooks/useWeather.ts`, 빈 폴더 `logistics/types`, `logistics/utils`; `src/app/api/weather/route.ts`(주석만 정리)
**변경 내용:** 신규 3탭 밀크런으로 이관된 뒤 고아가 된 발주/CSV/센터 상수·계산기·날씨 훅을 제거해 유지보수 범위를 줄임.
**주의사항:** 과거 문서(`docs/logs` 등)에 삭제된 파일 경로가 남아 있을 수 있음.

### [2026-04-18] [쿠팡밀크런·3탭·Prisma·기상청]

**요청:** `쿠팡_밀크런_관리_Cursor_프롬프트 (1).md` 기준으로 쿠팡 밀크런 기능 재구성(비용 계산기·재작업일 날씨·기간별 조회), Supabase 저장 시 PM 전달 안내, 이전 구현 롤백 후 재작업 등 포함
**변경 파일:** `src/components/logistics/CoupangMilkrunPage.tsx`, `src/components/logistics/milkrun/MilkrunCalculatorTab.tsx`, `MilkrunWeatherTab.tsx`, `MilkrunHistoryTab.tsx`, `src/app/api/weather/route.ts`, `src/app/api/allocations/route.ts`, `src/app/api/allocations/[id]/route.ts`, `src/lib/db.ts`, `src/lib/paju.ts`, `src/lib/kma-grid.ts`, `src/lib/kma-time.ts`, `src/lib/kma-pcp-parse.ts`, `src/lib/kma-short.ts`, `src/lib/kma-mid.ts`, `src/lib/kma-rework-fetch.ts`, `src/lib/kma-daily-weather.ts`, `src/lib/milkrun-*.ts`, `src/data/milkrun-centers.json`, `prisma/schema.prisma`, `prisma/migrations/20260418040000_milkrun_allocations/migration.sql`, `prisma/migrations/migration_lock.toml`, `scripts/parse-milkrun-centers.ts`, `src/app/(dashboard)/layout.tsx`(Toaster), `.env.example`, `package.json`(Prisma 5.22, 스크립트 `db:generate`/`db:migrate`/`parse:milkrun-centers`)
**변경 내용:** (1) **비용 계산기** — 센터 검색·필터 단위 전체 선택/해제·즐겨찾기·센터별 파렛트·비중·총합·CSV·초안 localStorage·저장 시 `POST /api/allocations`. (2) **재작업일 날씨** — 발주일 `orderDate`로 D-2/D-1, 파주 격자 `nx=56, ny=131`, `diff≤3` 단기·그 외 중기(`getMidLandFcst`+`getMidTa`), `GET /api/weather?orderDate=`. (3) **기간별 조회** — `GET/POST /api/allocations`, `GET/DELETE /api/allocations/[id]`, Recharts 일별 비용. (4) **Prisma** — `Allocation`/`AllocationItem`, `@prisma/client@5.22.0`(Prisma 7 설정 이슈 회피). (5) 중간에 사용자 요청으로 OpenWeather·일부 파일 추가했던 시도는 **롤백(삭제)** 한 이력 있음.
**주의사항:** `npm run build`는 타 레포지토리 이슈(`useForecast` 등)로 실패할 수 있어 밀크런 변경과 무관할 수 있음. 밀크런 저장은 **`DATABASE_URL` + migrate** 필수. 센터 전량은 엑셀+`npm run parse:milkrun-centers`로 `milkrun-centers.json` 갱신 가능.

> > > > > > > origin/submain

### [2026-04-16] [ERP크롤·EC5프레임대기]

**요청:** EC5(신버전)에서 메뉴 선택 후 콘텐츠가 iframe/프레임으로 비동기 로드되어 품목코드 입력칸을 못 찾는 문제 해결
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `docs/logs/진희.md`
**변경 내용:** `prgId=E040702` 또는 `/ec5/` 기반 URL을 가진 콘텐츠 프레임이 나타날 때까지 대기한 뒤, 품목코드/날짜/조회/엑셀 동작을 해당 프레임 스코프에서 우선 수행하도록 수정함. 디버그 응답에 `ledger_frame_url`도 포함해 실제 선택 프레임을 확인 가능하게 함.
**주의사항:** 계정별 커스터마이징으로 프레임 URL 패턴이 달라질 수 있어, 필요 시 `waitForContentFrame`의 선택 기준을 추가 조정해야 함.

### [2026-04-16] [ERP크롤·품목코드셀렉터확장]

**요청:** 재고수불부 진입 후 `품목코드 입력칸을 찾지 못했습니다` 오류 해결
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `docs/logs/진희.md`
**변경 내용:** 재고수불부 진입 후 대기 시간을 추가하고 품목코드 입력 셀렉터 후보(`data-cid/id/name/placeholder/aria-label` 기반)를 확장했으며, 실패 시 해당 시점의 frame/input 힌트를 포함한 디버그 응답을 반환하도록 보강함.
**주의사항:** 이카운트 화면 구성 변경으로 실제 입력 필드가 사용자별 커스터마이징된 경우, 반환되는 `debug.frames[].inputs` 기준으로 셀렉터를 추가 조정해야 할 수 있음.

### [2026-04-16] [ERP크롤·로그인탭세션추적]

**요청:** 로그인 클릭 후 `context.pages()` 새 탭 감지, `logincc`/`[회사코드].ecount.com` 전환 추적, 쿠키/세션 기준으로 로그인 실패와 페이지 전환 실패를 분리
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `docs/logs/진희.md`
**변경 내용:** 로그인 버튼 클릭 직후 새 페이지 이벤트를 대기하고 모든 열려 있는 페이지 URL 중 앱 후보를 선택해 후속 동작 페이지를 전환하도록 보강함. 로그인 실패 판정 시 쿠키 존재 여부(`ecount.com` 도메인)와 선택된 페이지 URL/전체 페이지 URL 목록을 함께 반환해 계정 실패와 리다이렉트 실패를 구분 가능하게 개선함.
**주의사항:** 자동 로그인이 보안정책(QR/추가인증/IP 제한)으로 차단되는 계정은 쿠키 생성이 안 되어 로그인 실패로 반환되며, 이 경우 크롤링 코드 수정만으로는 해결되지 않음.

### [2026-04-16] [ERP크롤·로그인URL고정]

**요청:** 로그인 URL을 `https://login.ecount.com/Login/?lan_type=ko-KR/`로 변경하고 `logincc.ecount.com/ec5/view/app.login/erp_login` 상태에서도 정확한 실패 처리
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `docs/logs/진희.md`
**변경 내용:** 크롤링 로그인 진입 URL을 사용자 지정 URL로 고정하고, 로그인 후 URL이 `login.ecount.com`/`logincc.ecount.com`/`/app.login/erp_login`에 머무는 경우를 로그인 미완료로 판정해 메뉴 탐색 전에 실패 응답을 반환하도록 보강함.
**주의사항:** 로그인은 통과했지만 권한/보안정책으로 앱 화면 전환이 차단되면 여전히 로그인 미완료로 처리되며, 이 경우 debug의 `current_url`/`page_text_snippet` 확인이 필요함.

### [2026-04-16] [ERP크롤·로그인프레임대응]

**요청:** ERP 크롤링 시 `#com_code` 타임아웃(`회사코드 입력칸을 찾지 못했습니다`) 오류 해결
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `docs/logs/진희.md`
**변경 내용:** 이카운트 로그인 진입 URL을 `www.ecount.com/kr` 기준으로 조정하고, 로그인/메뉴/조회/엑셀 클릭 로직을 페이지+프레임 전체에서 다중 셀렉터로 탐색하도록 보강함. 실패 시 현재 URL을 포함해 원인 추적이 가능하도록 에러 메시지를 개선함.
**주의사항:** 이카운트 로그인 UI가 캡차/보안정책으로 추가 변경되면 셀렉터 후보를 추가로 갱신해야 하며, 환경변수(`ECOUNT_COMPANY_CODE`, `ECOUNT_USER_ID`, `ECOUNT_PASSWORD`)가 비어 있으면 동일 흐름에서 실패함.

### [2026-04-16] [총재고·중복합산유형엑셀화]

**요청:** 중복 품목코드는 합산 집계하고, 유형은 `국내생산/수입` 대신 엑셀 기준 `수입/제품/상품`으로 표시/필터링
**변경 파일:** `src/components/logistics/_data/dailyInventoryBase.ts`, `src/components/logistics/FilterBar.tsx`, `src/components/logistics/InventoryDashboard.tsx`, `src/components/logistics/InventoryTable.tsx`, `docs/logs/진희.md`
**변경 내용:** 엑셀 `품목코드_매핑`에서 `지엘_품목코드+지엘_품목명+유형` 기준으로 재고수량/재고금액을 합산 집계해 기본 데이터(123건)로 재생성했고, 대시보드/필터/뱃지를 엑셀 유형(`수입/제품/상품`) 기준으로 동작하도록 통일함.
**주의사항:** 동일 품목코드라도 `유형`이 다르면 별도 행으로 유지되며, 합산 기준을 `품목코드` 단독으로 바꾸려면 집계 키를 추가 조정해야 함.

### [2026-04-16] [총재고·순번원본복원]

**요청:** 총재고현황에서 순번을 엑셀 원본 `순번`으로 맞추고, 120건만 보이는 이유 확인
**변경 파일:** `src/components/logistics/_data/dailyInventoryBase.ts`, `src/components/logistics/InventoryDashboard.tsx`, `docs/logs/진희.md`
**변경 내용:** 기본 데이터 생성 방식을 코드/품명 합산 집계에서 엑셀 행 단위(원본 순번 유지)로 변경해 `dailyInventoryBase`를 144건으로 재생성하고, 테이블 `seq_no`를 `row.seqNo`로 직접 반영하도록 수정함.
**주의사항:** 동일 품목코드가 제조연도/규격 등으로 여러 행 존재할 수 있어, 현재 총재고현황에서도 동일 코드가 복수 행으로 표시될 수 있음.

### [2026-04-16] [총재고·일일재고기본자료]

**요청:** `일일재고_3시스템_품목코드매핑.xlsx`를 총재고현황 기본 자료로 사용하고, `지엘_품목코드=품목코드`, `지엘_품목명=품목명`, `재고수량=재고량`, `원가_일일재고*재고수량=재고금액` 매핑 적용
**변경 파일:** `src/components/logistics/_data/dailyInventoryBase.ts`, `src/components/logistics/InventoryDashboard.tsx`, `docs/logs/진희.md`
**변경 내용:** 엑셀 `품목코드_매핑` 시트에서 지엘 품목코드/품목명 기준으로 재고수량과 원가\*수량 금액을 집계한 기본 데이터(`dailyInventoryBase`)를 생성하고, 총재고현황이 해당 값을 기본 재고량·재고금액으로 우선 표시하도록 대시보드 병합 로직을 변경함.
**주의사항:** 기본 데이터는 현재 엑셀 스냅샷을 기준으로 생성된 정적 파일이므로, 엑셀이 갱신되면 `dailyInventoryBase.ts`를 다시 생성/갱신해야 최신 값이 반영됨.

### [2026-04-16] [총재고·이카운트API제거]

**요청:** 총재고현황에서 이카운트 API 연동 전부 제거, 엑셀(마스터) 기준으로 운영
**변경 파일:** `src/components/logistics/InventoryDashboard.tsx`, `InventoryTable.tsx`, `SummaryCards.tsx`, `_hooks/useInventory.ts`, 삭제 `_server/ecountInventory.ts`, `_actions/ecountBalances.ts`, `docs/logs/진희.md`
**변경 내용:** 서버 액션·OAPI 호출·자동/수동 재조회·디버그 UI를 제거하고, 표·요약은 Supabase 집계값(`current_qty`, `stock_amount`)만 사용하도록 정리함. 품목코드·명은 기존처럼 `product-master.json` 매핑 유지.
**주의사항:** 재고 수치를 엑셀에서만 관리하려면 JSON/DB 반영 프로세스를 별도로 맞춰야 함.

### [2026-04-16] [ERP크롤패널추가]

**요청:** `src/components/logistics/ErpCrawlPanel.tsx`를 지정된 UI/상태 규칙대로 신규 구현
**변경 파일:** `src/components/logistics/ErpCrawlPanel.tsx`, `docs/logs/진희.md`
**변경 내용:** 고정 시작일(`2026-04-08`)과 오늘 기본 종료일을 `useState`로 관리하고, 버튼 클릭으로 `/api/crawl/ecount` POST 호출 후 성공 시 저장 건수/성공 상태, 실패 시 에러 메시지를 표시하는 카드 UI를 shadcn 컴포넌트만으로 구현함.
**주의사항:** `erpCode`가 없으면 요청 바디에 `item_code: null`로 전달되며, 에러 문구는 API 응답(`error`/`message`)을 우선 노출함.

### [2026-04-16] [수불부·ERP패널요약출처]

**요청:** `StockMovementsTab.tsx`에 ERP 크롤 패널 연결, 실시간 재고 요약카드 4개, 수불부 `출처` 컬럼 추가
**변경 파일:** `src/components/logistics/StockMovementsTab.tsx`, `docs/logs/진희.md`
**변경 내용:** 선택 품목 상단에 `ErpCrawlPanel`을 배치하고 성공 시 `refetch`가 실행되도록 연결했으며, `open_qty/total_in/total_out` 기반 2x2 카드와 계산 안내 문구를 추가함. 수불부 테이블 우측에 `출처` 컬럼을 추가해 `erp_crawl/manual/excel_import` 값을 Badge 라벨(ERP/직접입력/임포트)로 표시하도록 반영함.
**주의사항:** `transactions` 조회 Row에 `source` 값이 없는 레코드는 `—`로 표시되며, 요약카드는 고정 기준일(`2026-04-08`)부터 당일까지 실시간 계산값을 사용함.

### [2026-04-16] [수불부오류·선택시조회]

**요청:** 수불부 오류 해결 및 스냅샷/크롤링 개념 설명
**변경 파일:** `src/components/logistics/StockMovementsTab.tsx`, `docs/logs/진희.md`
**변경 내용:** 품목 미선택 상태에서 `useStockMovements`가 실행되던 구조를 `SelectedItemLedger` 분리로 변경해 선택된 경우에만 조회되게 수정하고, 테이블 미생성 에러(`Could not find the table`)는 사용자 안내 문구로 치환해 화면 오류 체감을 줄임.
**주의사항:** 실제 Supabase에 `items`, `transactions`, `inventory_snapshots` 테이블이 없으면 데이터 조회 자체는 불가하며, 이 경우 안내 문구가 표시됨.

### [2026-04-16] [기초재고·엑셀매핑반영]

**요청:** `품목코드_재고_ERP매핑 (1).xlsx` 기준으로 품목코드별 기초재고 반영
**변경 파일:** `src/components/logistics/_data/openingStockByErpCode.ts`, `src/components/logistics/_hooks/useInventory.ts`, `src/components/logistics/_hooks/useStockMovements.ts`, `src/components/logistics/StockMovementsTab.tsx`, `docs/logs/진희.md`
**변경 내용:** 엑셀의 `ERP코드(78열)`와 `이월재고(9열)`를 기준으로 `openingStockByErpCode` 매핑 파일을 추가하고, 스냅샷이 없는 경우 `useInventory`/`useStockMovements`가 해당 매핑값을 기초재고로 사용하도록 연결함.
**주의사항:** 이 매핑은 엑셀 스냅샷 시점 고정값이므로, 엑셀이 갱신되면 `openingStockByErpCode.ts`도 같이 갱신해야 최신 기초재고가 반영됨.

### [2026-04-16] [기초재고·CA_G열기준변경]

**요청:** `품목코드_재고_ERP매핑 (1).xlsx`의 CA열 코드와 G열 재고수량으로 기초재고 매핑 재반영 + 입출고내역 시작일 4/9 변경
**변경 파일:** `src/components/logistics/_data/openingStockByErpCode.ts`, `src/components/logistics/StockMovementsTab.tsx`, `docs/logs/진희.md`
**변경 내용:** 기초재고 매핑값을 엑셀 `CA열(ERP코드)` + `G열(재고수량)` 기준으로 전량 갱신하고, `StockMovementsTab`의 수불 조회 기준 시작일을 `2026-04-09`로 조정함.
**주의사항:** 일부 품목은 G열 값이 음수일 수 있어 기초재고도 음수로 반영되며, 엑셀이 바뀌면 매핑 파일 재생성이 필요함.

### [2026-04-16] [입출고탭·런타임에러수정]

**요청:** 물류 화면 오류 해결
**변경 파일:** `src/components/logistics/StockMovementsTab.tsx`, `docs/logs/진희.md`
**변경 내용:** `useInventory`의 `error` 미구조분해로 발생한 `ReferenceError: error is not defined`를 수정하고, `items` 테이블이 없어도 `product-master.json` 폴백 목록으로 품목 검색이 가능하도록 보완함.
**주의사항:** Supabase `items/transactions/inventory_snapshots` 테이블이 없으면 실제 재고/수불 데이터는 비어 있고 검색은 마스터 기반으로만 동작함.

### [2026-04-16] [ERP크롤·드라이런모드]

**요청:** Supabase 없이 크롤링 기능 동작 여부만 검증할 수 있게 구현
**변경 파일:** `src/app/api/crawl/ecount/route.ts`, `src/components/logistics/ErpCrawlPanel.tsx`, `.env.example`, `docs/logs/진희.md`
**변경 내용:** `CRAWL_DRY_RUN=true` 환경변수일 때 `/api/crawl/ecount`가 Supabase 저장을 건너뛰고 크롤링/엑셀파싱 결과만 성공 응답하도록 추가했으며, 패널 성공 문구는 API 메시지(드라이런 안내 포함)를 표시하게 수정함.
**주의사항:** 실제 DB 저장 검증은 `CRAWL_DRY_RUN=false`에서만 가능하며, `item_code`가 없으면 400 에러를 반환함.

### [2026-04-16] [머지후누락모듈복구]

**요청:** 물류 화면 전반 TypeScript 모듈 없음 오류 해결
**변경 파일:** `src/components/logistics/*`(누락 파일 복원), `src/components/ui/label.tsx`, `src/app/(dashboard)/logistics/coupang-milkrun/page.tsx`, `src/app/(dashboard)/logistics/leadtime/page.tsx`, `src/app/api/crawl/ecount/route.ts`, `src/app/api/tracking/route.ts`, `src/app/api/weather/route.ts`, `docs/logs/진희.md`
**변경 내용:** `submain` 머지 후 stash 선택 복원 과정에서 빠졌던 물류 의존 파일들(`LogisticsPage`, `CoupangMilkrunPage`, `FilterBar`, `SummaryCards`, `product-master.json` 등)과 필요한 UI 파일을 stash에서 추가 복구해 TS2307 연쇄 오류를 해소함.
**주의사항:** 머지 직후에는 선택 복원 범위에 누락이 생길 수 있어 `npx tsc --noEmit`으로 모듈 해석 상태를 한 번 검증해야 안전함.

### [2026-04-16] [트래킹API·리다이렉트차단]

**요청:** 트래킹 API 기능 미동작 원인 해결
**변경 파일:** `src/middleware.ts`, `src/app/api/tracking/route.ts`, `docs/logs/진희.md`
**변경 내용:** 전역 미들웨어 매처가 `/api/*`까지 인증 리다이렉트하던 문제를 수정해 API 경로를 제외하고, 트래킹 외부 API 호출에 타임아웃을 추가해 지연 시 원인 메시지를 반환하도록 보강함.
**주의사항:** 외부 기관 API(유니패스/공공데이터) 응답 지연 시 15초 후 타임아웃 에러를 반환하며, 이 경우 재시도가 필요함.

### [2026-04-16] [총재고·페이지10건·속도설명]

**요청:** 이카운트 로딩이 느린 이유 설명, 10건씩이면 더 빠른지 + 적용
**변경 파일:** `src/components/logistics/InventoryDashboard.tsx`, `docs/logs/진희.md`
**변경 내용:** 서버에서 품목별 순차 호출·스로틀(250ms/청크 400ms)로 412를 완화하는 구조라 페이지당 건수를 줄이면 한 페이지 완료 시간이 짧아짐을 주석으로 남기고 `PAGE_SIZE`를 10으로 조정함. UI 문구의 고정 `15건`을 `${PAGE_SIZE}`/`{PAGE_SIZE}`로 통일함.
**주의사항:** 전체 목록을 모두 보려면 페이지 수는 늘어남(트레이드오프).

### [2026-04-16] [총재고·15건자동조회페이지]

**요청:** 페이지당 15건 표시 + 이전/다음 페이지 이동 시 자동 조회
**변경 파일:** `src/components/logistics/InventoryDashboard.tsx`, `src/components/logistics/InventoryTable.tsx`
**변경 내용:** 페이지네이션 상태를 대시보드로 이동해 페이지당 15건으로 고정하고, 페이지 전환 시 해당 페이지 품목코드 15건을 자동으로 이카운트 조회하도록 변경함.
**주의사항:** 같은 페이지는 중복 자동호출을 방지하며, 강제 재조회는 `현재 페이지 재조회` 버튼으로 가능함.

### [2026-04-16] [이카운트412·스로틀완화]

**요청:** 이카운트 412 오류 반복 감소
**변경 파일:** `src/components/logistics/_server/ecountInventory.ts`
**변경 내용:** 병렬 조회를 순차 조회로 전환하고 요청 간 지연(250ms/배치 400ms)을 추가했으며, 412 발생 품목은 10분 쿨다운 캐시에 넣어 재호출을 차단함.
**주의사항:** 412는 이카운트 요청 제한 이슈라 완전 제거가 아니라 완화이며, 실패 메시지는 `요청 제한(412)`로 축약되어 표시됨.

### [2026-04-16] [총재고표·10건페이지네이션]

**요청:** 총재고현황에서 한 페이지당 10개씩 보이도록 변경
**변경 파일:** `src/components/logistics/InventoryTable.tsx`
**변경 내용:** 필터 적용 결과를 페이지당 10건으로 나눠 렌더링하고, 이전/다음 버튼과 페이지 정보(총 건수/현재 페이지)를 추가함.
**주의사항:** 검색/유형 필터가 바뀌면 페이지는 1로 초기화됨.

### [2026-04-16] [재고량전용·호출량완화]

**요청:** 품목코드 기준 API 조회로 재고량만 표시하고 412 실패를 줄이기
**변경 파일:** `src/components/logistics/InventoryDashboard.tsx`, `src/components/logistics/_server/ecountInventory.ts`
**변경 내용:** 자동 연속 호출을 제거하고 현재 필터 결과만 수동 조회하도록 변경했으며, 이카운트 반영값은 `BAL_QTY`(재고량)만 덮어쓰게 수정함. 서버는 412 발생 시 남은 품목 조회를 즉시 중단하도록 보완함.
**주의사항:** 실패가 많이 나오면 필터로 품목을 좁혀 소량씩 조회해야 하며, 412는 이카운트 측 호출 제한 정책 영향임.

### [2026-04-16] [이카운트·zone바디정합]

**요청:** `ZONE 데이터 입력 오류` 로그인 실패 해결
**변경 파일:** `src/components/logistics/_server/ecountInventory.ts`
**변경 내용:** Zone API로 조회한 `zone` 값을 로그인 호스트와 로그인 바디(`ZONE`)에 동일하게 사용하도록 수정해 ZONE 불일치 오류를 제거함.
**주의사항:** Zone API가 실패하면 `.env.local`의 `ERP_ZONE` 폴백을 사용하며, 이 값도 실제 ZONE 형식이어야 함.

### [2026-04-16] [이카운트·zone검증차단]

**요청:** Zone/Login 실패 반복 방지 정책 반영 및 연동 안정화
**변경 파일:** `src/components/logistics/_server/ecountInventory.ts`
**변경 내용:** 로그인 전에 Zone API(`oapi.ecount.com/OAPI/V2/Zone`)로 ZONE을 선검증하고, 인증 실패 누적 시(3회) 5분 차단 로직을 추가해 반복 실패 호출을 방지함.
**주의사항:** Zone API가 실패하면 `.env.local`의 `ERP_ZONE`으로 단일 폴백하며, 인증 정보가 계속 틀리면 일정 시간 자동 차단 메시지가 노출됨.

### [2026-04-16] [총재고·이카운트자동조회]

**요청:** 총재고현황의 재고량/재고금액을 이카운트 API에서 자동으로 가져오도록 변경
**변경 파일:** `src/components/logistics/InventoryDashboard.tsx`
**변경 내용:** 품목 목록이 로드되면 `loadEcountInventoryBalancesAction`을 자동 호출해 `BAL_QTY`/금액을 표와 요약 카드에 반영하도록 수정하고, 버튼은 수동 재조회 용도로 유지함.
**주의사항:** 이카운트 환경변수(`ERP_COM_CODE`, `ERP_USER_ID`, `ERP_API_KEY`, `ERP_ZONE`)가 비어 있거나 코드 매핑이 없으면 실패 건수가 증가함.

### [2026-04-16] [품목마스터·json고정fallback]

**요청:** 엑셀 접근 이슈 없이 총재고현황이 무조건 보이게 수정
**변경 파일:** `src/components/logistics/_data/product-master.json`, `src/components/logistics/InventoryDashboard.tsx`
**변경 내용:** `품목코드_매핑결과.xlsx`를 JSON(`product-master.json`)으로 변환해 프로젝트 내부 정적 데이터로 사용하고, Supabase `items` 미존재 시에도 JSON 품목목록으로 표를 렌더링하도록 변경함.
**주의사항:** 엑셀을 수정했으면 JSON 재생성이 필요하며(현재 126건), 실재고/입출고는 여전히 Supabase 테이블이 있어야 반영됨.

### [2026-04-16] [엑셀읽기·node런타임강제]

**요청:** 프로젝트 내부 엑셀 파일이 있어도 `Cannot access file ...xlsx` 오류가 지속되는 문제 해결
**변경 파일:** `src/components/logistics/_server/productMasterXlsx.ts`
**변경 내용:** `xlsx` import 방식을 Node `createRequire` 기반으로 교체하여 서버에서 CJS 런타임(`readFile`)을 확실히 사용하도록 수정함.
**주의사항:** 이 수정 후 엑셀 접근 오류는 해소되며, 남은 에러는 Supabase 물류 테이블(`items`, `transactions`) 미생성 이슈임.

### [2026-04-16] [품목마스터경로·고정]

**요청:** 품목 마스터를 `src/components/logistics/_data/품목코드_매핑결과.xlsx` 파일만 사용하도록 고정
**변경 파일:** `src/components/logistics/_server/productMasterXlsx.ts`
**변경 내용:** 엑셀 탐색 경로를 단일 고정 경로로 변경하고, 파일 미존재 시 안내 문구도 해당 경로 기준으로 정리함.
**주의사항:** `.env.local`의 `PRODUCT_MASTER_XLSX_PATH` 값은 현재 로직에서 사용하지 않음.

### [2026-04-16] [품목코드엑셀·헤더호환]

**요청:** 프로젝트 내부에 넣은 `품목코드.xlsx`가 적용되지 않는 문제 해결
**변경 파일:** `src/components/logistics/_server/productMasterXlsx.ts`
**변경 내용:** 엑셀 컬럼명을 `품목코드/ERP코드/PROD_CD`, `품목명/품명/상품명/ITEM_NM`까지 호환하도록 확장하고, 품목명이 없으면 품목코드를 임시명으로 사용하게 보완함. 기본 탐색 경로에 `_data/품목코드.xlsx`도 추가함.
**주의사항:** 품목명이 없는 파일은 표에 코드가 품목명으로 표시되며, 정확한 명칭이 필요하면 품목명 컬럼이 포함된 마스터 파일로 교체해야 함.

### [2026-04-16] [이카운트·디버그가시화]

**요청:** API를 실제로 끌고 오는지 확인 가능한 디버그 UI 추가
**변경 파일:** `src/components/logistics/_actions/ecountBalances.ts`, `src/components/logistics/InventoryDashboard.tsx`
**변경 내용:** 서버 액션 응답에 품목별 디버그 목록(코드/성공여부/BAL_QTY/실패사유)을 포함하고, 총재고현황에 `이카운트 디버그 결과` details UI를 추가해 버튼 클릭 후 호출 결과를 즉시 확인 가능하게 함.
**주의사항:** 디버그 목록은 최대 100건까지만 UI에 표시하며, 실패 원인은 이카운트 응답 메시지 또는 환경변수 오류 문구를 그대로 노출함.

### [2026-04-16] [엑셀접근·테이블미존재완화]

**요청:** `Cannot access file ...xlsx`와 `public.items` 미존재 오류 해결
**변경 파일:** `src/components/logistics/_server/productMasterXlsx.ts`, `src/components/logistics/_actions/productMasterMap.ts`, `src/components/logistics/InventoryDashboard.tsx`, `.env.example`
**변경 내용:** 엑셀 경로를 다중 후보(환경변수/기본/프로젝트 내부)로 탐색하고 읽기 권한 검사·원인 안내를 추가했으며, Supabase `items` 테이블이 없어도 엑셀 품목코드/품목명으로 총재고현황 표가 표시되도록 fallback 데이터를 연결함.
**주의사항:** 외부 경로 접근이 막히는 환경에서는 `PRODUCT_MASTER_XLSX_PATH`를 프로젝트 내부 경로로 설정해야 안정적으로 동작함.

### [2026-04-16] [품목마스터·엑셀매핑]

**요청:** `품목코드_매핑결과.xlsx` 기준으로 총재고현황의 품목코드/품목명 표시
**변경 파일:** `src/components/logistics/_server/productMasterXlsx.ts`, `src/components/logistics/_actions/productMasterMap.ts`, `src/components/logistics/InventoryDashboard.tsx`, `.env.example`
**변경 내용:** 서버에서 로컬 엑셀(`PRODUCT_MASTER_XLSX_PATH`, 기본 `C:/Users/user/Downloads/품목코드_매핑결과.xlsx`)을 읽어 `품목코드→품목명` 매핑을 생성하고, 대시보드에서 코드/품목명을 마스터 기준으로 덮어쓰도록 연결함.
**주의사항:** DB의 `items.erp_code`와 엑셀 `품목코드`가 일치해야 적용되며, 파일 경로가 다르면 환경변수로 경로를 지정해야 함.

### [2026-04-16] [이카운트·재고현황API]

**요청:** 이카운트 재고현황 API로 가져올 수 있는 데이터 반영 및 구현
**변경 파일:** `src/components/logistics/_server/ecountInventory.ts`, `_actions/ecountBalances.ts`, `InventoryDashboard.tsx`, `InventoryTable.tsx`, `SummaryCards.tsx`, `_hooks/useInventory.ts`, `.env.example`
**변경 내용:** OAPI 로그인 후 `ViewInventoryBalanceStatus`로 품목별 `BAL_QTY`(·가능 시 `BAL_AMT`)를 조회하는 서버 액션을 추가하고, 총재고현황에서 버튼으로 재고량·재고금액을 덮어씀. 품목명·유형·7일 예정은 기존 Supabase 로직 유지.
**주의사항:** `ERP_ZONE`은 계정 ZONE(예: CC)이어야 하며, 품목코드는 `items.erp_code`와 이카운트 `PROD_CD`가 일치해야 함. PM 영역 `src/app/api` 대신 서버 액션 사용.

### [2026-04-16] [총재고현황·컬럼]

**요청:** 총재고현황 탭 표 컬럼을 순번·품목코드·품목명·유형·재고량·재고금액·입고예정(7일)·출고예정(7일) 순으로 정리
**변경 파일:** `src/components/logistics/InventoryTable.tsx`
**변경 내용:** 실물/ERP/차이 3열을 `재고량`(`current_qty`) 한 열로 통합하고, `erp_code`를 품목코드 전용 열로 분리함.
**주의사항:** 상세 패널·훅의 `erp_qty`/`diff`는 그대로 두었으며, 표에서만 ERP·차이 비표시.

### [2026-04-16] [요약정리]

**요청:** PM 머지 정정 이후부터의 채팅 핵심 내용을 MD 파일로 정리
**변경 파일:** `src/components/logistics/PM_MERGE_AFTER_CHAT_SUMMARY.md`
**변경 내용:** 훅 완성, 물류 UI 재구성, 설정 구현, 탭 통합, 네비/라우트 정리까지의 핵심 결정과 결과를 한 문서로 요약함.
**주의사항:** 문서는 대화 요약본이며 실제 최종 코드 기준은 각 파일의 최신 상태를 우선 확인해야 함.

### [2026-04-15] [BL조회·파주입고실제자동]

**요청:** `⑤ 파주 창고 입고 실제`를 유니패스 `반출신고` 날짜로 자동 반영
**변경 파일:** `src/app/api/tracking/route.ts`, `src/components/logistics/_hooks/useLeadTime.ts`
**변경 내용:** 트래킹 API가 유니패스 상세 이력에서 `반출신고/수입신고수리/반출` 키워드 이벤트의 처리일을 `warehouseInDate`로 반환하도록 확장하고, BL 조회 저장 시 `step5_actual`에 자동 반영되게 연결함(값이 있으면 `current_step`도 5로 갱신).
**주의사항:** 유니패스 상세 이력 텍스트 형식이 BL마다 다를 수 있어 해당 키워드가 없으면 `step5_actual`은 자동 반영되지 않음.

### [2026-04-15] [BL조회·유니패스입항실제일]

**요청:** 인천 입항 실제일을 해양청 의존 없이 유니패스 API 이력 기준으로 자동 반영
**변경 파일:** `src/app/api/tracking/route.ts`
**변경 내용:** 유니패스 상세 이력(`cargCsclPrgsInfoDtlQryVo`)의 처리일(`prcsDttm`/`rlbrDttm`)과 이벤트 텍스트를 파싱해 입항/하선/반입/도착 키워드가 있는 시점을 `actualArrival`로 사용하도록 추가하고, 해양청 실입항(`etryndDt`)이 있으면 그 값을 우선 반영하도록 병합 처리함.
**주의사항:** 유니패스 이력 텍스트에 입항성 키워드가 없는 BL은 가장 이른 처리일을 보수적으로 사용하므로, 케이스에 따라 실제 입항일과 차이가 날 수 있음.

### [2026-04-15] [BL조회·출항입항자동반영]

**요청:** HBL 조회 시 선박명뿐 아니라 `③ 상하이 출항`, `④ 인천 입항` 날짜도 자동 반영
**변경 파일:** `src/app/api/tracking/route.ts`, `src/components/logistics/_hooks/useLeadTime.ts`
**변경 내용:** `/api/tracking` 응답에 유니패스 `etprDt` 기반 `departureDate`를 추가하고, BL 조회 저장 시 `step3_actual`에 출항일을 반영; 해양청 ETA가 비어도 `출항일 + 해상운송일`로 `step4_expected`를 계산해 인천 입항 예상일이 보이도록 보완함.
**주의사항:** `step4_actual`(실입항)은 해양청 응답(`etryndDt`)이 있어야 자동 채워지며, 공공데이터 키/호출부호/항만청코드 조건이 맞지 않으면 비어 있을 수 있음.

### [2026-04-15] [BL조회·HBL재시도]

**요청:** `SDLINC2602010` 조회 시 "조회 완료됐지만 선박/일정을 못 받음" 메시지 발생 원인 해결
**변경 파일:** `src/app/api/tracking/route.ts`
**변경 내용:** 유니패스 조회를 `mblNo` 단일 호출에서 `mblNo → hblNo` 순차 재시도로 확장하고, `tCnt=0` 응답을 성공으로 처리하지 않도록 보완해 HBL 기반 BL도 정상 매칭되게 수정함.
**주의사항:** 해양청 ETA/입항일은 여전히 `PUBLIC_DATA_API_KEY`와 `clsgn` 유무에 따라 비어 있을 수 있으며, 유니패스에 미등록된 BL은 502와 안내 문구로 반환됨.

### [2026-04-15] [트래킹API]

### [2026-04-15] [리드타임·BL자동연도]

**요청:** BL 조회가 안 되고 입항연도 입력을 사용자에게 노출하지 않기
**변경 파일:** `src/app/api/tracking/route.ts`, `src/components/logistics/_hooks/useLeadTime.ts`, `src/components/logistics/LeadTimeTracker.tsx`
**변경 내용:** `LeadTimeTracker`에서 입항연도 입력 UI를 제거하고 BL만으로 조회하도록 변경; `saveBL`도 `year` 인자 없이 호출; `/api/tracking`은 BL 문자열에서 연도 후보를 자동 추출하고 현재/전후 연도로 유니패스를 재시도한 뒤 가장 유효한 결과를 사용하도록 수정.
**주의사항:** 유니패스/공공데이터 키가 없거나 BL 자체가 미등록이면 자동 연도 탐색 후에도 빈 결과가 나올 수 있음.

### [2026-04-15] [리드타임·라벨예상일]

**요청:** 단계명 변경(선적 ETD/입항 ETA→상하이 출항/인천 입항), 예상일 적용 방식 문의
**변경 파일:** `src/components/logistics/LeadTimeTracker.tsx`
**변경 내용:** 단계 라벨을 `③ 상하이 출항`, `④ 인천 입항`으로 변경; `③ 상하이 출항`의 예상일이 비어 보이던 문제를 보완하기 위해 `④ 인천 입항 예상일 - 해상운송일` 기준으로 예상일을 역산해 표시하도록 수정.
**주의사항:** `④ 인천 입항` 예상/실제일이 모두 비어 있으면 `③ 상하이 출항` 예상일도 계산 근거가 없어 비어 있음.

### [2026-04-15] [요약문서]

**요청:** 채팅/코드 핵심 내용만 요약한 md 파일 생성
**변경 파일:** `src/components/logistics/LEADTIME_CHAT_SUMMARY_2026-04-15.md`
**변경 내용:** 수입 리드타임 관련 요구사항, 실제 반영 파일/로직, 현재 동작 방식, 남은 과제를 한 번에 확인할 수 있는 요약 문서를 작성함.
**주의사항:** HBL→MBL 자동 변환 로직은 아직 미구현이며 후속 작업 필요.

### [2026-04-15] [트래킹API]

**요청:** `GET /api/tracking` 라우트 생성, 제공 코드 기반 유니패스·외항반출입 연동
**변경 파일:** `src/app/api/tracking/route.ts`
**변경 내용:** BL·연도 쿼리로 유니패스 XML 파싱 후 선박명이 있으면 `PUBLIC_DATA_VESSEL_API_URL`·`PUBLIC_DATA_API_KEY`로 2차 조회, 응답은 `LeadTimeTracker`가 기대하는 `vesselName`·`eta`·`actualArrival`·`trackingStatus` 형식으로 반환.
**주의사항:** 공공데이터 API는 명세에 맞게 `.env`의 `PUBLIC_DATA_VESSEL_API_URL`(오퍼레이션 전체 경로)과 실제 파라미터명을 맞춰야 하며, 응답 JSON 구조가 다르면 `eta`/`actualArrival` 파싱을 조정해야 함.

### [2026-04-15] [트래킹API·명세반영]

**요청:** DAT211·유니패스 연계가이드(docx) 기준으로 `/api/tracking` 보완
**변경 파일:** `src/app/api/tracking/route.ts`
**변경 내용:** 유니패스는 `shipNm`/`csclPrgsStts` 등 가이드 태그로 파싱, 해양수산부는 `CargFrghtOut4/Info4` XML·`clsgn`·`prtAgCd` 등 DAT211 파라미터로 호출·`aprtfEtryptDt`·`etryndDt`에서 날짜 추출, 호출부호는 쿼리·env·유니패스 XML 후보 태그로 보강.
**주의사항:** 인천 등 항만청코드는 `PUBLIC_DATA_PRT_AG_CD`로 조정; 호출부호 없으면 2차 조회는 생략됨.

### [2026-04-15] [오후]

**요청:** 물류 사이드바에 `쿠팡 밀크런 관리` 메뉴 추가 후, 발주 입력(수동/CSV), 작업 일정(재작업일+날씨), 밀크런 최적화(팔렛/비용 계산) 전체 기능 구현
**변경 파일:** `src/components/layout/nav-logistics.ts`, `src/components/layout/Sidebar.tsx`, `src/app/(dashboard)/logistics/coupang-milkrun/page.tsx`, `src/components/logistics/CoupangMilkrunPage.tsx`, `src/components/logistics/CoupangMilkrunDialogs.tsx`, `src/components/logistics/constants/coupangProducts.ts`, `src/components/logistics/constants/coupangCenters.ts`, `src/components/logistics/types/milkrun.ts`, `src/components/logistics/utils/milkrunCalc.ts`, `src/components/logistics/_hooks/useWeather.ts`, `src/app/api/weather/route.ts`, `.env.example`, `.env.local`, `package.json`, `package-lock.json`
**변경 내용:** 쿠팡 밀크런 전용 라우트/메뉴를 추가하고, 상수/타입/계산 유틸을 분리한 뒤 수동 입력 모달·CSV/XLSX 업로드·발주 카드 선택·작업 일정 탭(기상청 프록시 기반 날씨 조회)·밀크런 최적화 탭(충진율/비용 계산, 미등록 품목 수동 팔렛 입력)을 구현함.
**주의사항:** 날씨 기능은 `KMA_SERVICE_KEY` 설정이 필수이며, CSV 품목 매핑은 업로드 파일의 품목명 표기 방식에 따라 일부 수동 보정이 필요할 수 있음.

### [2026-04-15] [오후]

**요청:** 기존 밀크런 기능은 유지하고, 별도 하위 탭으로 센터별 팔렛/비용 계산기 추가
**변경 파일:** `src/components/logistics/CoupangMilkrunPage.tsx`, `src/components/logistics/MilkrunCenterCalculator.tsx`
**변경 내용:** 선택 발주의 총 팔렛트를 기본값으로 받아 센터별 총 운송비(`팔렛트 수량 × 센터 단가`)를 순위 테이블로 보여주는 `센터별 비용 계산기` 하위 탭을 신규 추가함.
**주의사항:** 계산기는 단가 고정 기준 시뮬레이션이며, 실제 배차/혼재/차량 제약 조건은 반영하지 않음.

### [2026-04-15] [리드타임·BL조회피드백]

**요청:** BL 조회 후 화면 변화 없음
**변경 파일:** `src/components/logistics/LeadTimeTracker.tsx`, `src/components/logistics/_hooks/useLeadTime.ts`
**변경 내용:** `useEffect([selected])`가 조회 직후 `blMessage`를 지우던 문제를 `selectedId` 기준으로 분리; 조회 성공했으나 선박·일정이 비었을 때 안내 문구 표시; 유니패스 `blYy`용 입항연도 입력 추가 및 `saveBL(id, bl, year)` 지원.
**주의사항:** 여전히 키·BL·연도·clsgn에 따라 API는 빈 값을 줄 수 있음.

### [2026-04-15] [리드타임·MOCK]

**요청:** DB 구축 전 API·UI 검증 방법(더미 여부)
**변경 파일:** `src/components/logistics/_hooks/useLeadTime.ts`, `src/components/logistics/LeadTimeTracker.tsx`, `.env.example`
**변경 내용:** `NEXT_PUBLIC_LEADTIME_MOCK=true` 시 Supabase 없이 시드 1건·메모리 갱신으로 화면 동작, BL 조회는 실제 `GET /api/tracking` 호출; 상단에 MOCK 안내 배너 표시.
**주의사항:** 배포·실DB 연동 전 env 플래그 제거.

### [2026-04-15] [리드타임·탭분리]

**요청:** 수입 리드타임을 좌측 사이드바 별도 메뉴로 분리
**변경 파일:** `src/app/(dashboard)/logistics/page.tsx`, `src/app/(dashboard)/logistics/leadtime/page.tsx`, `src/components/logistics/LeadTimeTracker.tsx`, `src/components/layout/nav-logistics.ts`, `src/components/layout/Sidebar.tsx`
**변경 내용:** `/logistics/leadtime` 전용 페이지 추가, 물류 네비에「수입 리드타임」+ `Ship` 아이콘, `/logistics`에서는 리드타임 블록 제거; `LeadTimeTracker`에 `variant="page"` 지원; `/logistics`만 정확히 일치할 때 총재고 메뉴 활성(하위 경로와 겹침 방지).
**주의사항:** `Sidebar`는 레이아웃 공용 파일이라 PM 정책과 충돌 시 조정 필요.

### [2026-04-15] [리드타임·UI]

**요청:** DB 오류 시 리드타임 섹션이 화면에 거의 안 보이는 문제
**변경 파일:** `src/components/logistics/LeadTimeTracker.tsx`
**변경 내용:** `error && data.length===0`일 때 `<p>` 한 줄만 쓰던 분기를 제거하고, 빈 목록 레이아웃(제목·다시 불러오기·발주 추가·안내 카드·EmptyState)을 항상 표시함.
**주의사항:** 근본 원인은 Supabase에 `import_leadtime` 등 테이블이 없을 때의 스키마 오류임.

### [2026-04-15] [리드타임·조회실패]

**요청:** 리드타임 BL 조회가 자꾸 실패하는 원인 완화
**변경 파일:** `src/app/api/tracking/route.ts`, `src/components/logistics/_hooks/useLeadTime.ts`
**변경 내용:** 유니패스 `ntceInfo`만 있고 502 주던 로직을 가이드대로 `tCnt=-1`일 때만 오류로 처리하고, 다건 응답은 첫 `cargCsclPrgsInfoQryVo`에서 선박명·상태 파싱; 조회 실패 시 API JSON의 `error` 문구를 그대로 표시.
**주의사항:** 인증키 오류·BL 미존재는 여전히 실패하며, 해양수산부 ETA는 `clsgn`·`PUBLIC_DATA_API_KEY` 필요.

### [2026-04-15] [리드타임]

**요청:** 물류 리드타임 트래커(`import_leadtime`) — `useLeadTime` 훅, `LeadTimeTracker` UI, `/logistics` 페이지에 배치
**변경 파일:** `src/components/logistics/_hooks/useLeadTime.ts`, `src/components/logistics/LeadTimeTracker.tsx`, `src/app/(dashboard)/logistics/page.tsx`
**변경 내용:** 단계별 실제일·파라미터 저장, BL 조회(`/api/tracking`), 승인·테이블·엑셀 버튼(안내)을 구현했고, PM 타입에 테이블이 없어 Supabase `from`은 `never` 단언으로만 연결함.
**주의사항:** Supabase에 `import_leadtime` 테이블·RLS가 있어야 하며, `/api/tracking`은 PM 구현 전까지 BL 조회가 실패할 수 있음.

### [2026-04-15] [탭통합]

**요청:** `/logistics` 단일 페이지에서 총재고현황·입출고내역·설정 탭 통합, 사이드바는 총재고만, 설정 라우트 폴더 삭제, 행 클릭 시 입출고 탭+품목 선택
**변경 파일:** `src/components/logistics/LogisticsPage.tsx`, `StockMovementsTab.tsx`, `InventoryDashboard.tsx`, `LogisticsSettings.tsx`, `src/app/(dashboard)/logistics/page.tsx`, `src/components/layout/nav-logistics.ts`, 삭제: `src/app/(dashboard)/logistics/settings/`
**변경 내용:** `Tabs`로 3패널 구성, `StockMovementsTab`에서 검색·`TransactionLedger`, 상위에서 `selectedItem`과 탭 전환 연동, `InventoryDashboard`는 `ItemDetailPanel` 제거 후 `onItemClick`만 전달.

### [2026-04-15] [밤·설정]

**요청:** 물류 설정 페이지 `LogisticsSettings`에 엑셀 임포트(POST `/api/import/excel`)·ERP 동기화(GET `/api/erp/sync`)·`erp_sync_log` 마지막 시각 표시, `settings/page.tsx`는 배치만
**변경 파일:** `src/components/logistics/LogisticsSettings.tsx`, `src/app/(dashboard)/logistics/settings/page.tsx`
**변경 내용:** Card 2섹션으로 나누고 API 존재 여부는 GET 프로브로 판별(404면 비활성·안내), 임포트/동기화 결과는 문자열 state로 표시함.
**주의사항:** `erp_sync_log`·임포트 API는 PM 스키마/라우트가 있어야 동작함.

### [2026-04-15] [밤·패널]

**요청:** `ItemDetailPanel`·`TransactionLedger`·`TransactionForm`·`ScheduledPanel`·`ErpComparison`을 `logistics/` 루트에 두고 Sheet/Tabs/훅·Supabase 연동, ERP는 API 없을 때 비활성·안내
**변경 파일:** `src/components/logistics/ItemDetailPanel.tsx`, `TransactionLedger.tsx`, `TransactionForm.tsx`, `ScheduledPanel.tsx`, `ErpComparison.tsx`, `InventoryDashboard.tsx`, `src/components/ui/sheet.tsx`, `checkbox.tsx`, `label.tsx`, `tooltip.tsx`, 삭제: `logistics/components/` 내 동명 5파일
**변경 내용:** 우측/하단 반응형 `Sheet`, 상단 `SheetClose`, 예정 추가는 `Dialog`, 거래 저장은 `Button`+`Checkbox`·`Label`, ERP 동기화·원장은 `fetch`로 탐지 후 툴팁/문구 처리. Supabase는 `@/lib/supabase/client`의 `createClient` 사용(`supabase.ts` 없음).

### [2026-04-15] [저녁]

**요청:** 물류 페이지를 `page.tsx` 배치 전용으로 두고 `InventoryDashboard.tsx`에 로직 집중, `SummaryCards`·`FilterBar`·`InventoryTable`을 `logistics/` 루트로 이동·요구사항 반영
**변경 파일:** `src/app/(dashboard)/logistics/page.tsx`, `src/components/logistics/InventoryDashboard.tsx`, `src/components/logistics/SummaryCards.tsx`, `src/components/logistics/FilterBar.tsx`, `src/components/logistics/InventoryTable.tsx`, 삭제: `LogisticsDashboard.tsx`, `components/{FilterBar,SummaryCards,InventoryTable}.tsx`
**변경 내용:** `selectedItem`·`filter`·`isPanelOpen` 상태와 `useInventory`·당일 거래 집계를 대시보드에 두고, 요약 카드(Skeleton)·필터(엑셀은 console.log)·테이블(클라이언트 필터·Badge 색·Skeleton 5행)을 분리함. `InventoryFilter` 타입은 순환 참조 방지를 위해 `FilterBar.tsx`에서 export.
**주의사항:** 엑셀 다운로드는 PM의 `/api/export/excel` 추가 후 연결 필요.

### [2026-04-15] [오후]

**요청:** `src/components/logistics/` 전체를 Supabase 훅 기반 UI로 교체(덮어쓰기), 금지 영역(`src/lib`, `src/types`, `src/app/api`, `package.json`) 미수정
**변경 파일:** `src/components/logistics/LogisticsDashboard.tsx`, `LogisticsSettings.tsx`, `src/components/logistics/components/*`(전부), `src/app/(dashboard)/logistics/page.tsx`, `src/app/(dashboard)/logistics/settings/page.tsx`, `src/components/logistics/_hooks/useInventory.ts`(예정 row 필드), `src/components/logistics/_hooks/useStockMovements.ts`(unit_price)
**변경 내용:** 삭제됐던 `app/` 미러 구조 대신 대시보드·설정 페이지 컴포넌트를 루트에 두고, `useInventory`/`useStockMovements`·직접 Supabase 호출로 테이블·폼을 연결함. shadcn/ui(Table·Button·Card·Tabs·Select 등)로 교체하고 엑셀 추출은 클라이언트 `xlsx`로 처리함.
**주의사항:** 물류 테이블이 `supabase/types`에 없어 일부 `insert`/`update`는 타입 단언(`as never`)을 사용함. DB 컬럼·RLS는 PM 스키마와 맞춰야 함.

### [2026-04-15] [오전]

**요청:** 물류 훅 `useInventory.ts`, `useStockMovements.ts` 스켈레톤을 Supabase 기준 조회·계산 로직으로 완성
**변경 파일:** `src/components/logistics/_hooks/useInventory.ts`, `src/components/logistics/_hooks/useStockMovements.ts`
**변경 내용:** `createClient`로 items·스냅샷·거래·예정 테이블을 조회해 실물재고·ERP차이·7일 예정 입출고·금액을 계산하고, 기간별 원장 요약·누적 잔액을 반환하도록 구현함. 물류 테이블이 아직 `supabase/types`에 없어 훅 내부 Row 타입을 정의해 클라이언트 타입만 확장함.
**주의사항:** 프로젝트에 `src/lib/supabase.ts`는 없어 `@/lib/supabase/client`를 사용함. DB 컬럼명·enum이 실제 스키마와 다르면 PM 쪽 타입/마이그레이션과 맞춰 조정 필요.

### [2026-04-14] [오후]

**요청:** `src/components/logistics/` 내부에 GL 창고 재고 관리 앱 구조(앱/API/DB/재고계산/엑셀/ERP/UI/스토어) 생성
**변경 파일:** `src/components/logistics/lib/db.ts`, `src/components/logistics/lib/inventory-calc.ts`, `src/components/logistics/lib/erp-client.ts`, `src/components/logistics/lib/excel-import.ts`, `src/components/logistics/lib/excel-export.ts`, `src/components/logistics/store/inventory.ts`, `src/components/logistics/components/InventoryTable.tsx`, `src/components/logistics/components/ItemDetailPanel.tsx`, `src/components/logistics/components/TransactionLedger.tsx`, `src/components/logistics/components/TransactionForm.tsx`, `src/components/logistics/components/ScheduledPanel.tsx`, `src/components/logistics/components/ErpComparison.tsx`, `src/components/logistics/components/FilterBar.tsx`, `src/components/logistics/components/SummaryCards.tsx`, `src/components/logistics/app/layout.tsx`, `src/components/logistics/app/page.tsx`, `src/components/logistics/app/items/[id]/page.tsx`, `src/components/logistics/app/settings/page.tsx`, `src/components/logistics/app/api/items/route.ts`, `src/components/logistics/app/api/items/[id]/route.ts`, `src/components/logistics/app/api/transactions/route.ts`, `src/components/logistics/app/api/transactions/[id]/route.ts`, `src/components/logistics/app/api/scheduled/route.ts`, `src/components/logistics/app/api/erp/sync/route.ts`, `src/components/logistics/app/api/erp/ledger/[id]/route.ts`, `src/components/logistics/app/api/import/excel/route.ts`, `src/components/logistics/app/api/export/excel/route.ts`
**변경 내용:** 요청한 스키마 기준 SQLite 자동 마이그레이션과 재고 계산 함수(`getCurrentQty`, 7일 입출고 예정), ERP/엑셀 연동 유틸, CRUD API, 대시보드 UI 컴포넌트 및 상태 스토어를 물류 폴더 내부에 미러 구조로 구성함.
**주의사항:** 실제 Next 라우팅 경로(`/app`)로 동작시키려면 PM 영역인 루트 `src/app` 및 `src/lib` 연동이 추가로 필요하며, 현재 구현은 팀원 경계 준수를 위해 `src/components/logistics/` 내부 전용 구조임.

### [2026-04-14] [오후]

**요청:** 엑셀 시트 인덱스 기반(`품목 마스터`, `자사 일별 입출고`) 파싱 로직 및 `/api/import/excel` 임포트 순서/결과 응답 구현, 설정 페이지 임포트 버튼 추가
**변경 파일:** `src/components/logistics/lib/excel-import.ts`, `src/components/logistics/app/api/import/excel/route.ts`, `src/components/logistics/app/settings/page.tsx`
**변경 내용:** 헤더 3행/데이터 4행 기준으로 컬럼 인덱스 매핑 파서를 작성하고, items 업서트→snapshot 저장→transactions 저장 순서의 트랜잭션 임포트 API를 구현했으며, 설정 페이지에서 초기 임포트 호출/결과 표시를 추가함.
**주의사항:** 현재 버튼은 서버 경로 `C:/Users/user/Desktop/GL/GL/gl-project-master-data-v4_(2).xlsx`를 기본으로 호출하므로 배포/경로 변경 시 API 요청 바디의 `workbookPath`를 환경에 맞게 조정해야 함.

### [2026-04-14] [오후]

**요청:** `app/page.tsx` 중심으로 상단 네비/요약카드/필터바/재고테이블(TanStack v8)/슬라이드오버 상세패널 구성 및 `GET /api/items` 응답 스펙 확장
**변경 파일:** `src/components/logistics/app/page.tsx`, `src/components/logistics/components/SummaryCards.tsx`, `src/components/logistics/components/FilterBar.tsx`, `src/components/logistics/components/InventoryTable.tsx`, `src/components/logistics/components/ItemDetailPanel.tsx`, `src/components/logistics/store/inventory.ts`, `src/components/logistics/app/api/items/route.ts`, `src/components/logistics/app/api/export/excel/route.ts`, `src/components/logistics/app/items/[id]/page.tsx`
**변경 내용:** 대시보드 UI를 요구사항 기반으로 재구성하고 필터 상태(검색/국내생산/수입), 엑셀 추출(현재 필터 query), 50개 페이지네이션, 행 클릭 슬라이드오버를 반영했으며 API에서 `current_qty`, `erp_qty`, `diff`, `stock_amount`, `in_7days`, `out_7days`를 포함하도록 확장함.
**주의사항:** 현재 상단 메뉴 링크와 API 경로는 물류 폴더 내부 미러 구조 기준이며, 루트 App Router 경로와 1:1 연결은 PM 영역(`src/app`) 연동이 필요함.

### [2026-04-14] [오후]

**요청:** 실제 작업 경로 기준(`src/components/logistics/`, `src/app/(dashboard)/logistics/page.tsx`, `src/components/layout/nav-logistics.ts`)으로 연결
**변경 파일:** `src/app/(dashboard)/logistics/page.tsx`, `src/components/layout/nav-logistics.ts`
**변경 내용:** 대시보드 물류 페이지가 `src/components/logistics/app/page.tsx`를 직접 렌더링하도록 연결하고, 사이드바 물류 메뉴를 총재고/입출고예정/설정 항목으로 확장함.
**주의사항:** 현재 `/logistics/settings`는 별도 루트 페이지가 없어 링크 클릭 시 경로 연결이 추가로 필요하며, API는 기존 물류 폴더 미러 구조 기준이라 실제 루트 API(`/api/*`) 연동 작업이 후속으로 필요함.

### [2026-04-14] [오후]

**요청:** `ItemDetailPanel.tsx` 슬라이드오버(420px) + 탭 구조, `TransactionLedger.tsx` 회계장부형 원장, `GET /api/items/[id]/transactions` 응답 구현
**변경 파일:** `src/components/logistics/components/ItemDetailPanel.tsx`, `src/components/logistics/components/TransactionLedger.tsx`, `src/components/logistics/app/api/items/[id]/transactions/route.ts`, `src/components/logistics/components/InventoryTable.tsx`, `src/components/logistics/app/api/items/route.ts`, `src/components/logistics/app/page.tsx`
**변경 내용:** 상세 패널에 헤더/재고요약/4개 탭 UI를 추가하고, 원장 탭에서 기간 선택·기초/입출고/기말 요약·이월재고 행·누적잔액·ERP 반영 점 표시가 동작하도록 구성했으며, 기간 파라미터 기반 요약/원장 API를 추가함.
**주의사항:** `TransactionLedger`는 `/api/items/[id]/transactions` 경로를 호출하므로 루트 API 연결이 없으면 실제 페이지에서 404가 발생할 수 있으며, 현재 구현은 물류 폴더 미러 API 기준임.

### [2026-04-14] [오후]

**요청:** 탭2 직접입력(`TransactionForm.tsx`)과 탭3 입출고예정(`ScheduledPanel.tsx`) 구현 및 관련 API 확장
**변경 파일:** `src/components/logistics/components/TransactionForm.tsx`, `src/components/logistics/components/ScheduledPanel.tsx`, `src/components/logistics/components/ItemDetailPanel.tsx`, `src/components/logistics/components/TransactionLedger.tsx`, `src/components/logistics/app/api/transactions/route.ts`, `src/components/logistics/app/api/scheduled/route.ts`, `src/components/logistics/app/api/scheduled/[id]/route.ts`
**변경 내용:** 직접 입력 폼 필드(유형/일자/수량/거래처 자동완성/단가/적요/ERP반영)를 추가하고 저장 시 원장탭 자동 전환+리프레시를 연결했으며, 예정 목록 조회/추가/상태변경/삭제 UI 및 `GET/POST/PUT/DELETE` API를 구현함.
**주의사항:** 현재 호출 경로는 `/api/*` 기준이며 실제 라우트가 물류 폴더 미러 구조에만 있으면 런타임에서 경로 불일치가 발생할 수 있어 루트 API 경로 연결이 필요함.

### [2026-04-14] [오후]

**요청:** `lib/erp-client.ts` 및 ERP 연동 API(`sync`, `ledger`) 구현, 탭4 ERP 비교 UI 반영, `.env.local` 항목 추가
**변경 파일:** `src/components/logistics/lib/erp-client.ts`, `src/components/logistics/app/api/erp/sync/route.ts`, `src/components/logistics/app/api/erp/ledger/[id]/route.ts`, `src/components/logistics/components/ErpComparison.tsx`, `src/components/logistics/components/ItemDetailPanel.tsx`, `src/components/logistics/app/page.tsx`, `.env.local`
**변경 내용:** 이카운트 v2 세션 발급/1시간 캐시 및 `STOCK_QTY`, `LEDGER` 호출 클라이언트를 구현하고, 로컬 DB의 `erp_code` 보유 품목만 ERP 동기화하여 스냅샷 `erp_qty` 업데이트/동기화 로그 기록하도록 API를 구성했으며 ERP 비교 탭에서 경고 메시지·원장 테이블·재동기화 버튼을 동작하도록 연결함.
**주의사항:** `.env.local`의 ERP 값은 비어 있으므로 실제 연동 전 `ERP_COM_CODE`, `ERP_USER_ID`, `ERP_API_KEY`, `ERP_ZONE` 값을 입력해야 하며, 이카운트 응답 필드명이 계정별로 다를 수 있어 원장 테이블 컬럼 매핑 조정이 필요할 수 있음.

---

## [PM] [2026-04-20] 진희 PR #26 전체 분석 (오전)

**목적:** `team/진희` 브랜치를 `submain`으로 머지하기 전, 진희가 작업한 물류 메뉴(총재고 현황·수입 리드타임·쿠팡 밀크런·재작업일 날씨 등)를 CLAUDE.md 규칙·시스템 아키텍처·파일 구조·코드 정합성·Supabase DB 정합성 5가지 축으로 누락 없이 분석했습니다. 정민 PR과 동일한 방법론(쏘스맵 확인 → DB 실측 → 코드 품질 → 빌드 검증)을 적용했습니다.

### 1. 변경 파일 분류 (총 76개)

`git diff --name-status origin/submain...HEAD` 전수 확인 결과:

| 분류                     | 개수 | 설명                                                                                                                                                                                                                            |
| ------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 진희 고유 영역           | 33   | `src/components/logistics/**`, `src/app/(dashboard)/logistics/**`, `src/lib/logistics/**`, `src/components/layout/nav-logistics.ts`, `src/data/milkrun-centers.json`, `docs/logistics-*.md`, `docs/coupang-milkrun-features.md` |
| PM 영역 수정 (허용 범위) | 3    | `src/components/layout/Sidebar.tsx`(아이콘 3개 + NavItem 타입 + 헬퍼 함수), `src/app/(dashboard)/layout.tsx`(Toaster 추가), `package.json`(스크립트 1개 추가)                                                                   |
| 진희 작성 API (신규)     | 8    | `src/app/api/allocations/{,[id]}/route.ts`, `src/app/api/logistics/{coupang-inventory-upload,coupang-sku-narrative,gl-daily-series}/route.ts`, `src/app/api/tracking/route.ts`, `src/app/api/weather/{,forecast}/route.ts`      |
| Supabase 마이그레이션    | 5    | `supabase/migrations/202604181045~20260420120000_*.sql` (리드타임 테이블·컬럼·권한, 밀크런 allocations 2테이블, gl_warehouse_daily_series RPC)                                                                                  |
| 공유 유틸/훅/lib (허용)  | 10   | `src/lib/kma-*.ts`, `src/lib/paju.ts`, `src/lib/milkrun-compute.ts` 등 — 물류 전용으로 사용됨                                                                                                                                   |
| `scripts/` 파서          | 1    | `scripts/parse-milkrun-centers.ts` (엑셀→JSON 갱신)                                                                                                                                                                             |
| 실수 커밋                | 1    | 루트 `Untitled` 파일 — **삭제 필요**                                                                                                                                                                                            |
| 기타 (docs, 설정)        | 15   | CLAUDE.md, docs/**, .husky/** 등 (검토 필요하나 대부분 문서)                                                                                                                                                                    |

### 2. Supabase 의존도 실측 (Phase 3)

진희 코드가 참조하는 테이블/RPC를 `information_schema.tables`·`pg_proc`로 검증:

| 리소스                                | 코드 참조                           | DB 존재 여부                 | 비고                                 |
| ------------------------------------- | ----------------------------------- | ---------------------------- | ------------------------------------ |
| `allocations`                         | ✅ API 4곳                          | ✅ 존재                      | v_item_full 무관, 순수 신규          |
| `allocation_items`                    | ✅ API 2곳                          | ✅ 존재                      | CASCADE FK 설정됨                    |
| `inventory_operation`                 | ✅ 훅 3곳                           | ✅ 존재 (v6)                 | 쿠팡 일별 재고                       |
| `item_coupang_mapping`                | ✅ 훅 2곳                           | ✅ 존재                      | SKU↔item 매핑                        |
| `item_erp_mapping`                    | ✅ 훅 1곳                           | ✅ 존재                      | ERP 코드 매핑                        |
| `item_master`                         | ✅ 훅 1곳                           | ✅ 존재                      |                                      |
| `orders`                              | ✅ 훅 2곳                           | ✅ 존재                      |                                      |
| `sku_master`                          | ✅ API + 훅                         | ✅ 존재                      | 쿠팡 CSV 업로드 대상                 |
| `v_current_stock`                     | ✅ API + 훅                         | ✅ 뷰 존재                   |                                      |
| `import_leadtime`                     | ✅ 훅 `useLeadTime`                 | ✅ 존재 (22컬럼, 2건 저장됨) |                                      |
| **`gl_warehouse_daily_series`** (RPC) | ✅ `/api/logistics/gl-daily-series` | ❌ **DB에 없음**             | 마이그레이션 파일은 존재, 적용 안 됨 |

**핵심 발견:** `gl_warehouse_daily_series` RPC만 DB에 적용 안 됨 → GL 창고 일별 입출고 차트(Phase 3 검증 대상)는 현재 "일별 입출고 집계가 아직 준비되지 않았습니다" 에러 메시지만 출력하는 상태. 코드는 fail-safe하게 처리되어 앱 크래시는 없음.

### 3. 마이그레이션 적용 상태 (Phase 4)

`supabase_migrations.schema_migrations` 기록 조회 결과, 진희의 5개 마이그레이션은 **0개 기록**. 그러나 실제 DB에는:

- `import_leadtime` 테이블: ✅ 존재 (수동 실행 흔적, 22컬럼 일치, 2행 데이터)
- `allocations` / `allocation_items` 테이블: ✅ 존재 (스키마 일치)
- `import_leadtime` 권한 부여: 불명 (GRANT 조회 필요)
- `gl_warehouse_daily_series` RPC: ❌ 미존재

→ 머지 후 `npx supabase db push` 또는 수동 RPC 적용이 필요. 마이그레이션 멱등성(IF NOT EXISTS, OR REPLACE)은 잘 설계되어 재실행해도 안전.

### 4. API 엔드포인트 감사 (Phase 5)

진희 작성 8개 API 라우트 점검:

| 라우트                                         | 인증        | 환경변수 검증       | 에러/롤백                                             | 평가                |
| ---------------------------------------------- | ----------- | ------------------- | ----------------------------------------------------- | ------------------- |
| `POST /api/allocations`                        | ✅ getUser  | N/A                 | ✅ 자식 insert 실패 시 부모 delete 롤백               | 양호                |
| `GET /api/allocations`                         | ✅          | N/A                 | ✅ 요약·일별 집계 구조화                              | 양호                |
| `GET/DELETE /api/allocations/:id`              | ✅          | N/A                 | ✅ 404 처리                                           | 양호                |
| `POST /api/logistics/coupang-inventory-upload` | ✅          | ✅ SERVICE_ROLE_KEY | ✅ 12MB 제한·dedupe·배치 250                          | 양호                |
| `POST /api/logistics/coupang-sku-narrative`    | ✅          | ✅ LLM 키           | ✅ Anthropic/OpenAI 폴백                              | 양호                |
| `GET /api/logistics/gl-daily-series`           | ✅          | ✅ URL/KEY          | ✅ **RPC 없을 때 친절한 에러**로 graceful degradation | 양호(단 RPC 미적용) |
| `GET /api/tracking`                            | ✅          | ✅ UNIPASS_API_KEY  | ✅ 연도·BL 타입 후보 순차 시도, AbortTimeout 15초     | 양호                |
| `GET /api/weather`·`/weather/forecast`         | ❌ **없음** | ✅ KMA_SERVICE_KEY  | ✅ 5분 캐시                                           | **인증 체크 누락**  |

**권고:** `/api/weather*` 2개 라우트에 `getUser()` 인증 체크 추가. 현재는 무단 외부 호출 시 KMA 쿼터 소모 가능.

### 5. 코드 품질 (Phase 6)

| 항목                                    | 결과                                                                                                             |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 금지 라이브러리 (Prisma, MUI, axios 등) | ✅ **0건**                                                                                                       |
| Native HTML 사용                        | 4건 — `<input type="file">`(예외 허용), `<button>`×3(shadcn Button 대신 카드 내부 인라인 버튼·리스트박스 아이템) |
| `any`/`as any`                          | 2건 — allocations route의 `createClient() as unknown as SupabaseClient` (types.ts에 테이블 미반영 시 캐스팅)     |
| `console.*`                             | 가장 많은 파일: `useLeadTime.ts` 12건 — 전부 `console.error` 에러 로그, 무해                                     |

### 6. 컴포넌트 크기 (Phase 7)

| 파일                              | 라인 수 | 평가                                                              |
| --------------------------------- | ------- | ----------------------------------------------------------------- |
| `_data/dailyInventoryBase.ts`     | 995     | 데이터 파일이므로 정상                                            |
| `LeadTimeTracker.tsx`             | 800     | **분리 권장** (5단계 진행 + 수기 입력 + BL 조회 + 승인 로직 혼재) |
| `CoupangSkuAnalysisDialog.tsx`    | 549     | 분리 권장                                                         |
| `_hooks/useMilkrunAllocations.ts` | 456     | 조회·저장·삭제·CSV 모두 포함 — 분리 고려                          |
| `milkrun/MilkrunHistoryTab.tsx`   | 431     | 허용 범위                                                         |
| `_hooks/useLeadTime.ts`           | 400     | 허용 범위                                                         |

### 7. 공용 유틸 영향도 (Phase 8)

진희가 수정한 PM 영역 3개 파일, 모두 다른 팀원에 **영향 없음**:

1. `package.json` — `parse:milkrun-centers` 스크립트 1개 추가, 의존성 변동 없음
2. `src/app/(dashboard)/layout.tsx` — `<Toaster richColors position="top-center" />` 추가 → 모든 팀원이 `toast()` 사용 가능해짐 (긍정 영향)
3. `src/components/layout/Sidebar.tsx` — 아이콘 3개 + `NavItem` 타입 import + `resolveActivePathInGroup()` 헬퍼. 동일 그룹 내 중첩 경로 매칭 시 가장 긴 경로만 활성으로 표시 (기존 단순 매칭의 "부모+자식 동시 활성" 버그 수정) → 모든 메뉴에 긍정 영향

### 8. 타입 검증 (Phase 9)

`npx tsc --noEmit` 결과 **1건**:

```
src/lib/logistics/coupangSkuInsightRules.ts(205,52): error TS18048: 'r' is possibly 'undefined'.
```

원인: `const r = f.outbound_late_to_early_ratio;` — 타입은 `number | null | undefined`(optional 필드). `r !== null`만 체크해서 undefined가 남음.

수정 방법: `r !== null && r !== undefined` 또는 `r != null`로 한 줄 변경. 진희 영역 파일이므로 진희가 직접 고치거나 PM 권한으로 1줄 수정 가능.

### 9. 실수 커밋

- 루트 `Untitled` 파일: 1줄 내용(".env.local 안내" 중복 주석)으로 의미 없음. **삭제 필요**.
- 루트 `HANDOVER_2026-04-17_v6.md`: 현재 `git status`상 `D`(삭제 대기). 슬아 핸드오프 문서였고 완료되었으므로 삭제 진행.

### 10. 결론 / 머지 전 조치 체크리스트

**문제 없이 머지 가능한 항목:**

- 진희 영역 33개 파일, 신규 API 8개, 새 테이블 3개(allocations, allocation_items, import_leadtime)
- 공용 파일 3건 수정 모두 긍정적

**머지 전/직후 조치 필요:**

1. ✅ `Untitled` 파일 삭제
2. ✅ `HANDOVER_2026-04-17_v6.md` 삭제 확정(`git rm`)
3. ⚠️ `coupangSkuInsightRules.ts:205` 타입 에러 1건 수정 (1줄)
4. ⚠️ 머지 후 Supabase에 `gl_warehouse_daily_series` RPC 마이그레이션 적용 (`npx supabase db push` 또는 직접 SQL 실행)
5. ⚠️ `supabase_migrations.schema_migrations`에 진희 5개 마이그레이션 기록 보정(선택, 향후 CI 일관성 위해)
6. 권고(후속 PR): `/api/weather*` 인증 체크 추가, LeadTimeTracker·CoupangSkuAnalysisDialog 컴포넌트 분할
